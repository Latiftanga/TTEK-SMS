# TTEK-SMS — CLAUDE.md
# Read this file at the start of every session before writing any code.

## What this project is
Ghana GES-aligned School Management System for Tagnatek.
Multi-school SaaS platform. Every school feels like the system was built for them alone.

## Tech stack
- Backend:   FastAPI (async) + SQLAlchemy 2 + Alembic + PostgreSQL 16 + Redis + ARQ
- Frontend:  SvelteKit 2 + Tailwind v4 + Dexie.js + TanStack Query + Zustand
- PDF:       WeasyPrint (generated on demand, never stored)
- SMS:       SmsService driver abstraction (AfricasTalking, Hubtel, Arkesel, WiGal, Twilio)
- Storage:   Local /uploads now → Cloudflare R2 later (zero schema change)
- Mobile:    PWA now → Capacitor (Phase 7+) → React Native only if needed

## Absolute rules — never break these
- Every table has school_id (UUID FK → School) — RLS enforced at DB layer
- Grade is NEVER stored on Score — resolved at query time from GradingScale
- Fee balance is NEVER stored — computed live or read from StudentFeeSummary cache
- Class name is NEVER stored — computed: level + year_group + programme + stream
- AttendanceRecord always uses school_calendar_id FK — never a raw date field
- Offline score sync always sends offline_session_started_at — server checks for conflicts
- StaffPermission personal override always beats PositionPermission template
- Report cards generated on demand by WeasyPrint — never written to disk

## File size rule
No file exceeds 300 lines. If a file approaches 300 lines, split it before continuing.

## Folder structure
```
ttek-sms/
├── CLAUDE.md                          ← this file — read first every session
├── docker-compose.yml
├── .env.example
├── .gitignore
├── .github/workflows/ci.yml
├── blueprint/
│   └── ttek_sms_blueprint_v4.html     ← full architecture reference
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── alembic.ini
│   ├── alembic/versions/
│   └── app/
│       ├── main.py
│       ├── core/
│       │   ├── config.py              ← Pydantic Settings
│       │   ├── database.py            ← async engine + session
│       │   ├── redis.py               ← async Redis connection
│       │   ├── auth.py                ← JWT + session logic
│       │   └── permissions.py        ← 3-layer resolution + cache
│       ├── models/
│       │   ├── base.py                ← Base, TimestampMixin, school_id mixin
│       │   ├── school.py              ← Group 1: GhanaRegion, GhanaDistrict, School, SchoolConfig, SmsConfig
│       │   ├── auth.py                ← Group 2: User, UserSession, UserInvitation, StaffPosition, PositionPermission, StaffPermission, AuditLog
│       │   ├── staff.py               ← Group 3: StaffMember, StaffEmergencyContact, StaffPromotion, StaffQualification, StaffLeave
│       │   ├── academic.py            ← Group 4: AcademicYear, AcademicTerm, SHSProgramme, SubjectCatalogue, Subject, Class, ClassSubject, ClassTeacher, SubjectTeacher
│       │   ├── students.py            ← Group 5: Student, StudentMedicalRecord, Guardian, StudentGuardian, StudentEnrollment, TermEnrollment, SubjectRegistration, TransferRequest
│       │   ├── housing.py             ← Group 6: House, HouseMaster, Room, StudentHouseAssignment, NightRollCall, Exeat
│       │   ├── attendance.py          ← Group 7: SchoolSchedule, GhanaPublicHoliday, SchoolCalendar, SchoolPeriod, AttendanceRecord
│       │   ├── assessments.py         ← Group 8: GradingScale, Grade, AssessmentType, Assessment, Score, ScoreAuditLog, StudentBehaviourRecord
│       │   ├── fees.py                ← Group 9: FeeType, FeeStructure, StudentFeeRecord, FeePayment, FeeDiscount, FeeInstalmentPlan, StudentFeeSummary
│       │   └── documents.py           ← Group 10: DocumentRecord, ImportBatch, ImportRow, GraduationRecord, OfflineSyncConflict
│       ├── routers/                   ← one file per feature area
│       ├── services/                  ← business logic, one file per domain
│       ├── schemas/                   ← Pydantic schemas, one file per group
│       └── tests/                     ← mirrors routers/ structure
└── frontend/
    ├── Dockerfile
    ├── package.json
    └── src/
        ├── app.html
        ├── routes/
        └── lib/
            ├── api/                   ← API call functions
            ├── stores/                ← Zustand stores
            ├── components/            ← shared components
            └── offline/
                └── outbox.ts          ← Dexie WriteOutbox
```

## Current phase
Phase: 12 — Frontend Build
Status: IN PROGRESS
Started: 2026-06-13

### Phase 12 sub-phases completed
- 12a–c: SvelteKit foundation, PWA manifest, axios client, login flow, app shell, role-adaptive dashboard
- 12d: Multi-tenant subdomain detection, custom domain support, branded login, TopBar user menu
- 12e: Academic Setup (/admin/academic), Classes & Subjects (/admin/structure), Staff directory + detail (/admin/staff)
- 12f: Class detail page with card header, tabs (students/subjects/teachers), academic sidebar nav
- 12g: School Setup page (/admin/setup) — Profile, Subjects, Programmes, SMS, Email tabs; GET/PATCH /schools/me + /schools/me/logo backend endpoints; EmailConfig + EmailLog models + migration h6i7j8k9l0m1; email router; SMS Config moved from sidebar into Setup; Subjects/Programmes removed from Academic sub-nav
- 12h: Students module hardening — students.py split into students.py/students_enrollment.py/students_lifecycle.py/students_detail.py (300-line rule, students_detail's /{student_id} registered last in main.py to avoid path capture); admission_number auto-generation ({SCHOOL_CODE}/{YEAR}/{SEQ}, resets yearly, optional on create); primary-guardian invariant enforced on add/update/remove guardian (first guardian forced primary, last guardian can't be removed, primary removal auto-promotes, sole-primary demotion blocked); student photo wired end-to-end (POST/DELETE /students/{id}/photo, image pipeline in storage.py shared with school logo upload, PhotoAvatar.svelte); test coverage added for update_guardian, portal access grant/revoke, and all of the above (269 backend tests passing)
- 12i: Fee gate for term enrollment — AcademicTerm.block_owing_students toggle (+ set_by/set_at audit fields, migration f6a7b8c9d0e1) gates create_term_enrollment against the student's live StudentFeeSummary balance (never cached); waiver via TermEnrollmentCreate.fee_waiver_reason, honoured only if the caller's resolved permissions include fees.manage; bulk_term_enrollment skips fee-blocked students like it already did duplicates. Frontend: fee-gate toggle in admin/academic TermsSection, inline waiver form on EnrollmentTab's Register button, "Fee waived" badge on TermRegistrationRow, enrolled-vs-skipped counts in BulkActionModal's toast. Found and fixed a real bug during this work: bulk_term_enrollment's skip-on-duplicate path used a bare db.rollback(), which rolls back the whole session transaction (not just the failed item) — now each item runs in its own db.begin_nested() savepoint, matching register_subjects/student_import/staff_import/housing/promotion. Deliberately deferred: the instalment-plan "on schedule" carve-out (FeeInstalmentPlan.is_paid is never set anywhere in this codebase, so it would be a dead branch). 277 backend tests passing.
- 12j: Parent/student portal — the report-card viewing flow itself (scoring → approval → publish → PDF) was already solid and tested, but there was no way for a parent to actually reach it: an ADMISSION_ID login redirected to /dashboard same as staff, which silently rendered a blank teacher view for anyone without a staff_member_id, and there was no self-service endpoint for a portal user to discover their own name/class/enrollment_id. Added GET /portal/me + GET /portal/term-enrollments (schemas/portal.py, services/portal.py — is_report_published extracted and shared with the existing report-card endpoint). Frontend: new (portal) route group with its own minimal layout at /portal — profile card, per-term published/not-yet-published state, "View report card" opens the PDF (same blob pattern as the existing staff reports page); login redirects ADMISSION_ID users to /portal instead of /dashboard; (app) layout redirects a portal user away if they land on a staff route anyway. Verified live end-to-end: real student, granted portal access, logged in, confirmed is_published flips after staff publish an assessment, confirmed the PDF is retrievable. 8 new backend tests, 285 total passing; svelte-check 0 errors.
- 12k: Fee balance in the parent portal — GET /portal/term-enrollments now also returns fee_total_due/fee_total_paid/fee_balance/fee_last_payment_date per term, computed live from StudentFeeSummary (never cached; null when no fees assigned yet, not a zero balance) — folded into the existing term list rather than a new endpoint. Frontend: each term row on /portal shows "Balance due: GHS X" (red) or "Fees fully paid" (green) using the same ghs() formatter as the staff fees pages. Verified live: created a fee structure, bulk-assigned it, confirmed the portal showed the full balance, recorded a partial payment as staff, confirmed the portal balance updated live (600 → 350). 9 portal tests passing, 285 backend tests overall.
- 12l: Term results lock + behaviour audit trail — rejected calendar-day-type gating for scores/behaviour (would block legitimate Saturday classes, boarding incidents on holidays, etc.) in favour of workflow-state locking, matching the existing Assessment.is_published pattern. Added AcademicTerm.results_locked (+ results_locked_by_id/results_locked_at, mirroring the block_owing_students pattern from 12i; migration b1c2d3e4f5a6) which freezes submit_scores/approve_scores and behaviour record create/delete for every assessment in that term, independent of each Assessment's own is_published flag. Override requires assessments.approve_scores + a non-blank override_reason, written to ScoreAuditLog.reason (scores) or the new BehaviourAuditLog (behaviour — previously had no audit trail at all; SET NULL, not CASCADE, on the record FK so the log survives a delete). Added core/permissions.py::user_has_permission() (User → staff_member_id → resolve_permissions, mirroring student_enrollment.py's fee-waiver check) since submit_scores is gated at the router on the weaker enter_scores permission and needs to check for the stronger one internally. Also added a light term-bounds sanity check — Assessment.due_date and StudentBehaviourRecord.incident_date must fall within the enclosing academic_term's start_date–end_date — deliberately not calendar-day-type validation. 14 new tests (test_scoring_lock.py, test_behaviour_lock.py), 299 backend tests overall. Frontend: initially put the "Results lock" toggle next to "Fee gate" in admin/academic TermsSection, then relocated it to the /assessments list page (next to the class/term filters) — /admin/academic is roles:['admin']-only in nav.ts, but the override permission (assessments.approve_scores) maps to the 'approver' role (HOD/Exam Officer) who can't see that page at all; /assessments is already visible to teacher/admin/approver and is where the term-scoped workflow this lock belongs to actually happens. TermsSection keeps a read-only "Results locked" badge (points to Assessments to manage it) but no toggle, to avoid two controls for one piece of state. The assessment detail page shows a "Term locked" badge and, if Save/Approve gets a 423, opens the new reusable OverrideReasonModal to collect a reason and retry — extracted AssessmentActionsBar out of that page to stay under the 300-line cap while adding the logic. Verified live against the running stack: locked a term via PATCH (both from TermsSection and later from the /assessments toggle), confirmed submit/approve scores 423 without a reason and succeed with one, confirmed ScoreAuditLog.reason captured the override text; svelte-check 0 new errors.
- 12m: Behaviour records management page — the backend endpoints (POST/GET/DELETE /behaviour) existed since Phase 9 but had no frontend at all. Added new BehaviourTab on the student detail page (students/[id], alongside Profile/Guardians/Enrollment/Fees/Medical) rather than a class-wide list like /assessments, since incidents are logged one at a time against a specific student as they happen — term selector + "Log incident" form + list, mirroring FeesTab's term-selector pattern; reused OverrideReasonModal and ConfirmModal for the results_locked create/delete flow (create/delete both require assessments.approve_scores per the router, so only canManage = admin/approver sees the button and any 423 is purely "missing reason", never "missing permission"). Found the same nav mismatch as 12l: the top-level "Students" nav item was roles:['teacher','admin'] only, even though HOD/Exam Officer (the 'approver' role) already holds students.view/edit + assessments.approve_scores at the backend — added 'approver' to that one nav entry (children Transfers/Promotions/Graduation stay admin-only, unrelated to this permission). New frontend/src/lib/api/behaviour.ts API client. Verified live: created and deleted a behaviour record through the real endpoints, confirmed SSR of /students and /students/[id] still 200, svelte-check 0 new errors (only pre-existing @apply warnings, same pattern as every other tab file).
- 12n: Term lock extended to assessment edit/delete + AssessmentAuditLog — 12l's AcademicTerm.results_locked freeze only covered scoring/behaviour; update_assessment and delete_assessment could still mutate a locked term's assessments untouched. Promoted scoring.py's private _term_lock_override_reason into core/permissions.py::check_term_lock_override() so assessment.py could reuse it: update_assessment now needs the same assessments.approve_scores + non-blank override_reason to edit a locked term's assessment; delete_assessment has no override path at all (deleting one permanently removes its scores and their audit trail, so there's no safe "undo" to gate). A code review of this in-flight work caught a real bug: update_assessment validated the override_reason via check_term_lock_override() but then discarded the return value — no audit table existed for assessment field edits at all, unlike ScoreAuditLog/BehaviourAuditLog. Added AssessmentAuditLog (migration c2d3e4f5a6b7) — every name/max_score/due_date edit is logged with an old/new value snapshot, reason populated only on a locked-term override; SET NULL (not CASCADE) on the assessment FK so the log survives a delete. Frontend: the assessment detail page's edit form now goes through the same OverrideReasonModal flow as submit/approve. Verified live against the running stack (PATCH with/without override, confirmed audit rows and reason capture via direct DB query, confirmed the row survives assessment delete with assessment_id set to NULL); 359 backend tests passing overall.
- 12o: Guardian portal login (schema foundation) — added User.guardian_id (PHONE login type; a guardian can be linked to multiple children via StudentGuardian, so one account spans multiple students, unlike student_id's one-to-one) alongside staff_member_id/student_id, migration a8b9c0d1e2f3. The same migration retroactively created ck_user_admission_id_matches_login_type and a widened three-way ck_user_single_identity, both of which existed only in the SQLAlchemy model's __table_args__ since the Phase 1 baseline and were never actually applied to the database. Login/session wiring for the guardian PHONE flow itself is not yet built — this is the schema step only. While reviewing the notification path for the new portal type, found student_portal.py's _notify_guardian silently always failed: it imported a function that doesn't exist (sms_notifications.get_active_driver — the real name is _get_active_driver) and called _log_result with the wrong argument order, both swallowed by a broad except Exception. Fixed by routing the student ADMISSION_ID portal's notification through a new shared notify_portal_access() helper, built so the future guardian PHONE portal can reuse it too. Known limitation (documented on the model): phone is globally unique platform-wide like email, so a guardian with children at two different schools on this platform can't hold two separate guardian portal accounts under the same phone number — not solved, rare in practice, same constraint already applies to staff PHONE accounts.
- 12p: Two standalone fixes, unrelated to each other and to 12o's in-flight guardian-portal work. (1) Housemaster dashboard only ever showed one house: assign_house_master() (services/housing.py) deactivates the *previous* housemaster of the house being assigned but never checks whether the incoming staff member already runs a different house, so a staff member active as HouseMaster for two houses at once (same bug shape as TeacherDashboard's former my_class) had the second silently dropped. HousemasterDashboard.house_id/house_name/... (singular) replaced with my_houses: list[HouseSnapshot]; extracted services/dashboard_housemaster.py (dashboard.py was approaching the 300-line cap) mirroring the existing dashboard_admin.py/dashboard_teacher.py split. (2) Report cards couldn't actually be scanned: qrcode[pil] has been in requirements.txt since Phase 7 but nothing ever imported it — all three templates (basic/SHS/ECM) only printed the literal text "Verify: /verify/{token}". Added services/qr.py::generate_qr_image() (PNG data URI encoding {app_base_url}/verify/{token}) wired into report_card.py's context; templates now embed the image above the existing verify text. Verified live against the running stack: real TermEnrollment → assembled context → WeasyPrint-rendered PDF, confirmed the embedded PNG decodes and verify_token() round-trips it to the correct enrollment_id/school_id. 359 backend tests passing, svelte-check 0 new errors.
- 12q: Subject-registration eligibility for scoring — prompted by a report that a prior system auto-enrolled every class member in every subject assigned to the class, so electives (e.g. French vs Literature-in-French in the same class) silently got mixed up. A critical read of this codebase found the same failure mode one layer down: SubjectRegistration (per-student, per-term, added in 12i's EnrollmentTab) was recorded but never consulted anywhere — create_assessment didn't check subject_id belonged to the class, the assessment detail page's score-entry roster pulled listStudents({class_id}) with no subject filter at all, and submit_scores accepted any student_id in the payload. Fixed all three: create_assessment now requires subject_id to be an active ClassSubject on class_id; new GET /assessments/{id}/roster (services/subject_roster.py) returns only students eligible for the assessment's subject, and the assessment detail page now calls it instead of the plain class-wide student list; submit_scores rejects (422, naming the students) anyone not registered for that subject this term. Eligibility rule is deliberately backward-compatible: a student's registration only becomes authoritative once at least one SubjectRegistration row exists for their TermEnrollment that term — absence of registration data (no TermEnrollment yet, or one with nothing registered) falls back to "the whole class curriculum applies", so schools that never use subject registration (GES Basic, no electives) are unaffected; the entire pre-existing test suite passed unchanged with zero fixture changes beyond one (test_assessments.py's subject fixture needed a ClassSubject row to keep passing the new create_assessment guard). Split AssessmentType CRUD out of services/assessment.py into services/assessment_type.py first, to stay under the 300-line cap while adding the ClassSubject check. New test_subject_roster.py (5 tests) covers the elective-split scenario directly. 364 backend tests passing, svelte-check 0 new errors.
- 12r: Guardian portal login — completes 12o's schema-only User.guardian_id groundwork with the actual PHONE login flow and self-service portal. A guardian can be linked to multiple children via StudentGuardian, so this is a different shape from the student ADMISSION_ID portal (single implicit self): new services/guardian_portal.py grants/revokes portal access from a student's Guardians tab; unlike a student's admission number, a guardian has no safe predictable initial password, so grant_portal_access sets a random unusable one and SMS's the guardian to use "Forgot password?" with their own phone number — reuses the existing OTP reset flow (services/auth_reset.py) completely unchanged, no new onboarding-password mechanism needed. New GET /portal/children (guardian-only) lists linked students; the existing GET /portal/term-enrollments and GET /portal/report-cards/{id} now accept an optional student_id, required and StudentGuardian-verified for a guardian caller, ignored (resolves to self) for a student's own login — unchanged behaviour there. UserRead/frontend CurrentUser gained guardian_id, since PHONE login is shared with staff accounts and login_type alone can't tell the frontend whether to route to /portal or /dashboard. Guardian.phone doubles as the portal login identifier, so update_guardian now keeps an active portal account's User.phone in sync with edits, and both grant and update surface a clean 409 (not a raw 500) on the platform-wide phone-uniqueness collision documented as a known limitation since 12o. Frontend: GuardianPortalAccessButton on each guardian row; /portal shows a child switcher for guardians with 2+ children, unchanged single-profile view for one child or a student login (the two portal-read queries needed reactiveQuery() rather than plain createQuery({...}) since they gate on $currentUser, which hydrates asynchronously after mount — caught this in my own first draft before it shipped). 13 new backend tests (test_guardian_portal.py), 377 backend tests passing overall, svelte-check 0 new errors. Known remaining gaps, not addressed here: services/student.py is pre-existing debt at ~510 lines (over the 300-line cap, untouched since it was tangential to this feature); ranking (services/report_card.py::_compute_rank) still isn't elective-aware (flagged in 12q, a design decision deferred, not a clear bug).
- 12s: Split services/student.py (pre-existing debt flagged in 12r, ~510 lines) down to 278 — extracted student_display.py (shared read-only formatting helpers: _display_name, _class_display, _photo_url, _get_class_map, reused by student.py, student_list.py, student_export.py, student_custom_export.py, and services/portal.py), student_list.py (list_students() — collection search/filter/sort), and student_photo.py (upload/delete student photo). student.py keeps core Student CRUD + medical record upsert. Updated every external import site and both router call sites (routers/students.py, routers/students_detail.py) to point at the new modules — no backwards-compat re-exports left behind. Pure refactor, no behaviour change: 377 backend tests passing unchanged, svelte-check untouched (backend-only). Elective-aware ranking (services/report_card.py::_compute_rank, flagged in 12q) remains the one open item.
- 12t: Elective-aware class ranking — _compute_rank summed every subject a student had an approved+published score for, which unfairly compared students taking different electives (one student's total might include an easy elective, another's a hard one) and silently penalized a student with a still-pending score in one subject versus a classmate with a complete score set. Deferred from 12q since it needed its own design decision, walked through with the user using a concrete example (two students with identical core scores but different electives) before implementing. Fix: once a student's TermEnrollment has any SubjectRegistration rows recorded, only CORE-registered subjects count toward their rank total — electives still appear individually on the report card, they just don't feed the single class-position number. Backward compatible by the same rule as 12q: a TermEnrollment with zero registration rows (the common case for a school that never uses subject registration — GES Basic, no electives) falls back to summing every subject with a score, exactly as before. Split services/report_card.py (pushed to 336 lines by the change) into report_card_scoring.py (weighted-score aggregation, shared between assemble() and ranking) and report_card_rank.py (the ranking query itself, renamed compute_rank — no longer private since it's now cross-file), keeping report_card.py's own assemble() under the 300-line cap; the two new files had a natural circular-import risk (rank needs the scoring helper, scoring is also needed by assemble) resolved by making report_card_scoring.py a third shared leaf module both import from, same pattern as 12s's student_display.py. New test_report_card_rank.py (2 tests) calls compute_rank() directly since there's no JSON endpoint exposing rank/class_size (baked into the PDF only) — confirms zero behaviour change with no registration data, and confirms a student with the best shared-core scores now correctly outranks a classmate whose only edge was a strong elective (the first version of this test asserted a tied rank number, which failed — this ranking algorithm has never had a "tied rank" concept, ties are broken arbitrarily by sort order, so the test was rewritten to assert a meaningful ordering change instead). 379 backend tests passing overall.
- 12u: Assessment app review (asked for directly, not tied to a bug report) surfaced two real bugs by close reading rather than a failing test. (1) scoring.py::approve_scores resolved cached_grade_label straight from raw_score with no normalization against the assessment's own max_score — GradingScale bands are 0–100 percentage ranges (confirmed via the seeded GES scale and report_card_scoring.py's own "out of 100" comment), so any assessment not scored out of 100 (a 20-mark quiz, say) was graded as if the raw mark were a percentage: 18/20 (90%) landed in the 0–59.99 band and got stamped "F9", visible on both the live score table and the printed report card. Fixed by normalizing to a percentage before resolve_grade(); renamed that function's param raw_score→percentage with a docstring spelling out the expectation, so a future caller can't repeat the mistake silently. (2) report_card_rank.py::compute_rank's raw SQL never filtered is_active on student_class_assignment or term_enrollment, unlike every other read site touching those tables in this codebase — a student withdrawn or transferred mid-term (rows deactivated, not deleted, by student_lifecycle.py) stayed counted in class_size and the ranking pool on every classmate's report card generated afterward. Predates the 12t split (confirmed via git log), carried over rather than introduced by it. Two regression tests added; 351 backend tests passing. Commit 59f492c.
- 12v: Academic module review → subject/programme catalogue pickers → opt-in programme adoption model. Started as a review of academic.py/academic_class.py/academic_subjects.py/academic_structure.py, which found two bugs: (1) update_programme's "claim on first edit" mutated a shared (school_id=NULL) GES programme row in place — since the standard programmes are seeded once and shared platform-wide, any single SHS school editing one (even via the plain "Deactivate" button already sitting in ProgrammesTab.svelte) silently reassigned that row to itself, vanishing it from every other school's list with no warning to either school. (2) assign_subjects had no ownership check on subject_ids at all — Subject rows are school-private (unlike the shared SubjectCatalogue), so a class could be attached to another school's subject if the id were known, and a nonexistent id fell through to a raw 500 IntegrityError instead of a clean 404. Fixed both (subject check: 404 on cross-school/bogus id; programme fix, first pass: fork a school-owned copy on first edit rather than mutating the shared row) with regression tests, 355 passing. Commit d08e32e.
- 12w: Subject registration split — pre-existing uncommitted work found sitting in the working tree from before this stretch of sessions, reviewed and tested before committing rather than assumed clean (given how many real bugs this same review process had already turned up elsewhere). Split student_enrollment.py's register_subjects/list_subject_registrations/delete_subject_registration out into student_subject_registration.py (over the 300-line cap); register_subjects gained a class-eligibility check requiring subject_id to be an active ClassSubject on the student's class, mirroring create_assessment's guard from 12q; bulk_class_assignment gained the same per-item db.begin_nested() savepoint pattern used everywhere else for bulk operations (student_import/staff_import/housing/promotion/etc.) so one duplicate mid-batch can't poison the rest of the transaction. Found during the review: register_subjects didn't filter is_active on TermEnrollment or StudentClassAssignment — the same class of gap fixed in 12u/12v elsewhere — so a withdrawn/transferred student (rows deactivated, not deleted, by student_lifecycle.py) could still have new subjects registered against their stale enrollment. Fixed with a regression test. 357 backend tests passing. Commit 82d9fc4.
- 12x: CI infrastructure fix + ARQ bulk-report-job test coverage. GitHub Actions had been failing on every single run for weeks (confirmed back through 20+ runs in the Actions history) — the backend job spun up a bare postgres:16-alpine service container and ran pytest directly against it with no migration step at all, so every test errored at fixture setup (conftest.py's own docstring assumes "alembic upgrade head has already created all tables," a precondition CI never met; the local docker-compose dev stack never surfaced this since its Postgres is long-lived with migrations already applied once and persisted across sessions). A second, compounding gap: even with tables created, several fixtures depend on scripts/seed_reference_data.py's output (e.g. conftest.py's school fixture scopes itself to "whatever region/district exists in the seeded DB"), which CI also never ran. Added both missing steps to .github/workflows/ci.yml; verified by reproducing CI's exact setup locally first — a throwaway postgres:16-alpine container, same env vars, same two steps, full suite: 0 failures — before pushing, then confirmed the real GitHub Actions run went green (both Backend and Frontend jobs). While verifying, the coverage report — running for the first time now that CI actually completes — surfaced app/worker.py and services/bulk_report_job.py at 0%. worker.py is left uncovered deliberately (pure ARQ lifecycle wiring — startup/shutdown DB pings and the WorkerSettings registration — no business logic to test); bulk_report_job.py's real logic lives in the already-factored-out _run() (bulk_generate_report_cards is just the ARQ entrypoint that unwraps ctx["db"] and calls it, same split this codebase already uses for other job/pure-function pairs), so new test_bulk_report_job.py tests _run() directly against a real DB and real WeasyPrint rendering — one confirming a full class produces one PDF per student in the zip, one confirming a single student's assemble() failure (via monkeypatch — no natural fixture setup reproduces this without corrupting the whole batch) is captured as an error file and counted in `failed` without crashing or dropping the other students. 92% coverage on bulk_report_job.py (the 3 remaining lines are the ctx-unwrapping glue itself). 359 backend tests passing overall. Commits 1bc8ee7, aaaaea3.
- 12y: Student transcript — asked how to view/print a full academic record, for graduated and continuously-enrolled students alike (explicitly not gated on graduation status: "continuous students should also be able print all semesters they spent so far"). No transcript feature existed at all; report_card.py::assemble() only ever renders one TermEnrollment at a time. New services/transcript.py::assemble_transcript() spans every term a student has ever been enrolled in, batched to a constant ~6 queries regardless of history length (all TermEnrollments/StudentClassAssignments/Scores/attendance/behaviour fetched once each with `.in_(term_ids)`, then grouped by term in Python) rather than looping report_card.py's per-term query set N times — reuses report_card_scoring.py::_compute_weighted_scores() unmodified per term slice. Deliberately omits per-term class rank (report_card_rank.py::compute_rank is an expensive per-class query; calling it per term would reintroduce the exact N+1 explosion the batching avoids — cumulative record is the point, not per-term standing) and any QR/verify token (existing scheme verifies one TermEnrollment at a time; each underlying report card stays individually QR-verifiable as today). One unified templates/transcript.html (not a BASIC/SHS split — a transcript can span a student's whole time at the school), one section per academic year containing that year's terms. New GET /students/{id}/transcript (routers/students_transcript.py) uses assessments.view — matching report_cards.py's permission convention, not students.view, since this exposes score data. Separately, added a `graduated` filter to GET /students (student_list.py — EXISTS subquery against GraduationRecord.graduation_type == GRADUATED, lifetime check not scoped to one year) for the "find past graduated students" half of the ask; deliberately did not wire up the existing GET /students/graduation endpoint (permission-mismatched, wrong shape for the normal list UI). Frontend: "Download transcript" button on the student profile header (always visible, not conditional on is_active — the whole point of the fix); "Graduated" pill on the students list forces active_only=false in the same click, since most graduated students are is_active=False and the naive toggle would silently show zero results. Extracted StudentFilterBar.svelte out of students/+page.svelte (pushed to 305 lines by the new pill) to stay under the 300-line cap. 5 new backend tests (test_transcript.py, plus one in test_students.py), 375 backend tests passing overall, svelte-check 0 new errors. Verified live: pulled a real transcript PDF for a real demo student against the running stack.
- 12z: Year-end actions on the student profile's Enrollment tab ("as if they happen already, we can do better"). Root cause: ClassActionPanel.svelte (the per-student Promote/Repeat/Demote panel) called assignStudentToClass directly — the same divergence already fixed once this session in PromoteSection.svelte (class detail page): both offer identical Promote/Repeat/Demote actions, but only the bulk /admin/academic/promote flow and the fixed PromoteSection.svelte actually wrote a GraduationRecord via services/promotion.py. This per-student panel was a third, still-broken copy — it never wrote an audit record, so it also had no way to show "already processed" status. Fixed by rewiring its mutation to bulkPromoteStudents (single-item records array), matching PromoteSection.svelte exactly, and adding the "also register in first term" checkbox for parity with the bulk flows. Added a student_id filter to GET /students/graduation (services/graduation.py + routers/students_lifecycle.py — previously only filtered by academic_year_id, and was entirely unused by the frontend per 12y's note) so the panel can fetch just this student's own history. Frontend now shows a status banner when a GraduationRecord already exists for the current academic year ("Already promoted for 2025/2026 — using the same year again below will be skipped, not reapplied"), and an inline warning inside the promote/repeat/demote form itself once a target year with an existing record is selected, so staff aren't surprised when the confirm button's own toast says "skipped." 1 new backend test (test_list_graduation_records_filters_by_student), 376 backend tests passing overall, svelte-check 0 new errors. Verified live: promoted a throwaway test student via the same single-item bulkPromoteStudents call this panel now makes, confirmed the GraduationRecord was created and retrievable by student_id, confirmed a duplicate promote attempt was skipped not re-applied, cleaned up all test data afterward.
- 12aa: Subject registration term lock ("It will not make sense to add a subject to a student enrollment which is already past"). register_subjects()/delete_subject_registration() (student_subject_registration.py) had no awareness of AcademicTerm.results_locked at all — a subject could be added to or removed from a term enrollment whose results were already finalized and report cards possibly already generated, same nonsensical-once-closed problem the term lock already solves for scores/behaviour/assessment edits (12l/12m/12n). Fixed by reusing the existing shared core/permissions.py::check_term_lock_override() (same mechanism as scoring.py/assessment.py) rather than behaviour.py's lighter local _check_term_lock — subject registration is gated at the router by the weaker students.edit (not assessments.approve_scores like behaviour/scoring), so the override itself still needs the caller to hold assessments.approve_scores, same shape as submit_scores. Backend: POST /students/term-enrollments/{id}/subjects request body changed from a bare list to {items, override_reason} (SubjectRegistrationBulkCreate) to carry the reason; DELETE .../subjects/{id} takes override_reason as a query param, matching DELETE /behaviour/{id}'s existing convention. Frontend: extracted SubjectRegistrationPanel.svelte out of TermRegistrationRow.svelte (own add/remove state + queries, reused by both inline-tree and standalone-card render modes — TermRegistrationRow was pushed to 327 lines by the override-modal wiring, now just renders the collapsed row + a "Term locked" badge) — wired the existing OverrideReasonModal component (already used by BehaviourTab.svelte/assessment detail page) for both add and remove. Updated 4 pre-existing tests in test_student_enrollment.py to the new request body shape; added 5 new term-lock tests (blocked without reason, allowed with reason + assessments.approve_scores, reason alone insufficient without that permission, delete blocked/allowed the same way) — 381 backend tests passing overall, svelte-check 0 new errors. Verified live end-to-end against a throwaway academic year/term (to avoid locking a real shared demo term mid-session): registered a subject while unlocked, locked the term, confirmed a second registration attempt 423'd, confirmed override_reason pushed it through, confirmed delete followed the same block/override pattern; cleaned up all test data afterward.
- 12ab: Enrollment tab redesign ("I want us to redesign the student enrollment tab, I don't like it") — a full UX pass, not a bug fix, walked through with the user via AskUserQuestion before touching any code. Review found the tab was the one place on the student profile that broke the page's own conventions: FeesTab.svelte/BehaviourTab.svelte both use a flat term `<select>` defaulting to is_current; Enrollment instead forced a 3-level expand-drill (collapsible year/class card → flat term list, rendered two different ways depending on registered/not → a third level for subjects), no current-term indicator anywhere in the tree, an amber-alert empty state that looked like a different page, and ClassActionPanel's Promote/Repeat/Demote/Transfer sitting permanently open despite being a once-a-year action — unlike the sibling PromoteSection.svelte (class detail page), which already collapses that exact same panel behind a dashed trigger. User chose (via AskUserQuestion with ASCII previews): a single compact timeline — one row per term across every year, not nested under a year/class card; unregistered terms still shown as a row with a "Not registered" + Register action, not hidden; empty state restyled to the timeline's own card language instead of a separate alert box. Implemented as pure frontend restructuring, zero backend changes: repurposed TermRegistrationRow.svelte (which had a confirmed-dead "standalone" render mode, deleted) into new TermTimelineRow.svelte — current-term dot marker, subject count visible without expanding (queries the same `['term-subject-regs', id]` cache key SubjectRegistrationPanel.svelte already uses, so TanStack Query dedupes rather than double-fetching), an "Inactive assignment" tag for a stale/withdrawn class assignment's terms (an edge case the old tree gave no signal for at all), and the not-registered/fee-gate-waiver flow relocated from inline tree markup onto the row itself. EnrollmentTab.svelte became the assembly layer: restyled empty state, year-grouped timeline, and ClassActionPanel wrapped in a collapsed-by-default "Manage year-end status" trigger copied verbatim from PromoteSection.svelte's own pattern. SubjectRegistrationPanel.svelte and ClassActionPanel.svelte themselves untouched. svelte-check 0 new errors; verified live against the running demo stack (a real student with 2 years of history, and a throwaway no-class-assigned student for the empty state) — SSR 200, no console/API errors, cleaned up test data afterward.

  Follow-up in the same session ("On a term that is not active, add subject should not active too"): the new TermTimelineRow.svelte checked only whether a TermEnrollment existed, never its own is_active flag — a term the student withdrew from mid-term (TermEnrollment.is_active=false, distinct from the class-assignment-level "Inactive assignment" tag already added) still rendered the full "Registered" state with a working Add/Remove-subject panel, even though register_subjects() 404s server-side for an inactive TermEnrollment (confirmed live: created a throwaway enrollment, flipped is_active=false directly, POST .../subjects returned 404 "Term enrollment not found"). Fixed by deriving isRegistered = enrollment && enrollment.is_active and gating the subject-count/expand/SubjectRegistrationPanel on that instead of plain enrollment truthiness; the row now falls back to the same muted state as "Not registered" but labeled "Withdrawn from this term" so it reads differently from a term never touched at all — the Register button still works either way, since create_term_enrollment already reactivates an existing inactive row in place. svelte-check 0 new errors; cleaned up all test data afterward.
- 12ac: Subject-teacher assignment — two fixes to SubjectsTab.svelte (the class detail page's "Subjects" tab, the only place a subject-teacher assignment is made). (1) "subjects are assigned on academic basis not semester": SubjectTeacher was scoped to academic_term_id, forcing re-assignment every term even though ClassTeacher (the form/class teacher) was already year-scoped only — rescoped to academic_year_id, matching that convention directly (found a prior precedent for this exact migration shape already in the repo: y0z1a2b3c4d5_class_teacher_yearly.py did the same thing for ClassTeacher months ago). New migration d4e5f6a7b8c9 backfills academic_year_id from each row's academic_term, then (since the live dev DB has real data, unlike when ClassTeacher was migrated) dedupes any rows that collide once term granularity drops — a school that genuinely swapped teachers mid-year is a legitimate prior case, resolved by keeping is_active=true first, then the highest term_number (the most recent assignment) — before dropping academic_term_id entirely; downgrade() is documented as best-effort/lossy, it can't reconstruct collapsed per-term history. services/staff_responsibilities.py's subject-assignment query now joins AcademicYear directly instead of through AcademicTerm, making SubjectAssignment structurally identical to ClassTeacherAssignment. (2) "filter out members who are not teachers": the Add-subject and Change-teacher dropdowns listed every active staff member via listStaff() with no role filter — StaffCategory.staff_type (TEACHING/NON_TEACHING) already exists for exactly this (the model docstring literally says "shown in class teacher / subject teacher assignment dropdowns"), just never wired into this component. Added a client-side teachingStaff derived filter (staff_type === 'TEACHING' strictly, not null/NON_TEACHING) for both dropdowns; kept the underlying staffQ/staffMap unfiltered for read-only "already assigned" name lookups. 4 new backend tests (test_academic.py — year-scoped assignment, mid-year reassignment updates in place not duplicates, list filters by year, responsibilities reflects academic_year not a term), 385 backend tests passing overall, svelte-check 0 new errors. Verified live against the real dev DB: ran the migration against live subject_teacher rows (not just an empty test DB), assigned a real teaching staff member to a subject for the current year, confirmed it showed on their Responsibilities tab with a year not a term — and live data itself confirmed the second bug was real: a "Finance User" (NON_TEACHING) staff member was already assigned as a subject teacher from before this fix, exactly the case the new filter now prevents going forward. Cleaned up the one row created during verification; left the pre-existing one as found.
- 12ad: Grading scale seed data ("Why am I seeing on the grading scale nothing but on the report_card page I see Basic, SHS and early childhood?"). The two pages are unrelated — report-card format is a hardcoded 3-value UI enum, nothing to do with grading — but investigating "nothing" surfaced a real gap: the grading_scale table was completely empty for the entire database, not just this school, and resolve_grade() (services/grading.py) only ever checks the calling school's own scale with no fallback. Net effect discovered: no score in the whole system could be assigned a letter grade until a school manually created its own scale — GradingScale.school_id is nullable specifically so "system defaults exist alongside school-specific overrides" (the model's own docstring) like SubjectCatalogue/SHSProgramme/StaffPosition templates, but unlike those, nothing had ever seeded one. Fixed per the user's chosen direction (AskUserQuestion): seeded a shared "GES Standard Grading Scale" (school_id=NULL, 9 WASSCE-style bands A1-F9) in scripts/seed_reference_data.py, matching the file's existing idempotent check-by-name-then-insert pattern; resolve_grade() now falls back to the shared default when a school has none of its own (a school's own default, once created, still always wins — verified with a dedicated regression test); list_grading_scales() widened to include the shared scale so it's visible at all, ordered school's-own-first. get_grading_scale/update_grading_scale/add_grade/delete_grade deliberately untouched — they already filter on exact school_id match, which a NULL row can never satisfy, so the shared scale is naturally read-only through every edit endpoint with zero extra backend code; only had to make ScaleCard.svelte not offer those actions on it (a "System default" badge instead of Default/Set-default, no edit/add-band/delete-band controls, grades still shown read-only). 3 new backend tests, 388 backend tests passing overall, svelte-check 0 new errors. Verified live: ran the seed script against the real dev DB (confirmed idempotent — reran, 0 new), confirmed the demo SHS school (which owns zero scales) now sees the shared scale via GET /assessments/grading-scales where it previously saw nothing.
- 12ae: Attendance module review ("one of the critical features") — 5 findings, all fixed. (1) get_summary()/get_class_summaries() filtered their "total markable days" denominator by each calendar day's CURRENT day_type but never filtered the present/absent/late/excused counts (numerator) by day_type at all — since override_calendar_day() and generate_calendar(force=True) can both reclassify a day after attendance was already marked on it, a record could keep counting in the numerator after its day dropped out of the denominator, letting the rate exceed 100%. Fixed by adding the same day_type filter to both queries. (2) mark_attendance() never validated that class_id or any student_id belonged to the calling school — only school_calendar_id was checked; same cross-tenant gap shape as Staff/Academic earlier this session. Added a Class ownership check (404) and a batch Student ownership check (422, naming any cross-school ids) — confirmed live against a second real school (admin@basic.school) that a cross-school class_id now 404s. (3) generate_calendar(force=True) blindly overwrote every day in a term including ones staff had deliberately hand-corrected via override_calendar_day — added SchoolCalendar.is_manual_override (migration e5f6a7b8c9d0, simple additive boolean column, no backfill needed), set on override, and force-regeneration now skips any day flagged this way. CalendarGrid.svelte shows a small dot indicator on protected days. (4) Attendance marking had no AcademicTerm.results_locked gate unlike scores/behaviour/subject registration, despite attendance feeding directly into report cards — gated via the same shared core/permissions.py::check_term_lock_override() mechanism, AttendanceMarkRequest gained override_reason, attendance/+page.svelte wired the existing OverrideReasonModal (same isLocked/detailOf pattern as BehaviourTab.svelte). (5) report_card.py, transcript.py, and attendance.py's get_summary() each computed "attendance rate" with a different formula (days marked vs. every markable day in the term) and could disagree for the same student/term — unified onto one new shared services/attendance_stats.py::compute_attendance_stats(), day_type-consistent (fixing (1) at the source for these two consumers too), which report_card.py::assemble() and transcript.py now both call instead of each keeping their own inline query; _load_attendance() removed from report_card.py entirely. 8 new backend tests (cross-school class/student rejected, rate stays consistent after a day is reclassified — regression for (1) — force-regenerate preserves a manual override while still updating a non-overridden day, term-lock blocked/allowed/insufficient-permission three-case set mirroring test_scoring_lock.py, compute_attendance_stats() called directly), 396 backend tests passing overall, svelte-check 0 new errors. Verified live end-to-end: cross-school class rejected via a second real school, marked a real student present then reclassified that real calendar day and watched total_school_days AND days_present drop together (65→65, 1→0) instead of the old inconsistent behavior, locked a throwaway term and confirmed 423-without-reason/201-with-reason, pulled a real report card and transcript PDF to confirm the shared attendance-stats refactor renders correctly. All test/throwaway data cleaned up afterward, including resetting a real calendar day's is_manual_override flag that a verification step had flipped (no API existed to clear it, so a direct DB correction was needed — noted as a deliberate sticky-until-a-human-reverts-it design, not a follow-up bug).
- 12af: Attendance UI polish, 5 small fixes on top of 12ae ("Can we improve the UI to make more user friendly", scoped to Attendance via AskUserQuestion). (1) The calendar page's term `<select>` never marked which term was current, unlike every other term-picker in the app (Fees/Behaviour/Enrollment) — added the same `(current)` suffix. (2) The manual-override dot added in 12ae had no legend entry anywhere — added one, plus mentioned the protection directly in the "Regenerate" confirm text ("Days you've manually overridden...are protected and won't change") so the action reads as safe rather than scary. (3) Converted the calendar page's inline override form (which pushed the whole page down when a day was clicked) into a centered modal matching ConfirmModal.svelte's established skeleton (`use:portal`, Escape-to-close). (4) Added a "Today" shortcut next to the Mark page's date picker, only shown once you've navigated away from today. Extracted AttendanceStudentRow.svelte out of attendance/+page.svelte (pushed to 315 lines by the Today button) to stay under the 300-line cap — the per-student status-button row plus its STATUSES color map, parameterized by an onToggle callback so the parent keeps owning the "click active status again to clear it" logic. svelte-check 0 new errors; verified live (SSR 200, no console/API errors) — no backend changes, pure frontend.

  Asked what happens in the UI if the catalogue is seeded with 100 subjects, which surfaced a bigger gap: nothing changes, because SubjectsTab.svelte's "Add subject" was pure manual code+name entry — GET /academic/catalogue existed on the backend but nothing in the frontend ever called it. Built AddSubjectPanel.svelte: "From catalogue" (searchable, Core/Elective-filterable checkbox list defaulting to the school's own level via schoolType, bulk adopt) / "Custom subject" (the original manual form) toggle; new lib/api/academic.ts::listCatalogue(). Verified live against the real demo stack — real GES catalogue data, adopted two subjects, confirmed catalogue_id linkage, confirmed the custom path still worked, no API/frontend errors. Commit 9dec139.

  Asked to do "the same for programmes," which exposed a deeper design gap the first pass's fork-on-edit fix hadn't actually addressed: unlike Subject (a genuine adopt-a-copy split from SubjectCatalogue), SHSProgramme has no separate catalogue table — Class.programme_id references the shared rows directly, so every SHS school automatically saw and could use all 6 standard programmes with no way to not offer one. User clarified the real requirement with a concrete example: one SHS school should be able to run only Business while another runs everything, exactly like electives — confirming programmes needed the full adopt-your-own-copy treatment, not just a safer edit path. This superseded d08e32e's fork-on-edit fix rather than building on it: list_programmes now returns only a school's own adopted/custom rows; the shared rows are a read-only catalogue, browsed via new list_programme_catalogue and copied in via new adopt_programme (matched/deduped by code, since programmes have no catalogue_id FK the way subjects do — rejects re-adopting the same code with 409); update_programme now only matches a school's own rows, so editing the shared catalogue directly is no longer possible at all and the original mutate-the-shared-row failure mode can't recur. create_class/update_class — which had no ownership check on programme_id at all, or matched shared rows too — now require the programme to belong to the calling school, closing the gap that would otherwise let the adoption requirement be bypassed by referencing an unadopted or cross-school id directly. Found and fixed the same always-active-only bug in list_subjects while touching its sibling: a deactivated subject was permanently unreachable through the UI with no way to reactivate it. Built AddProgrammePanel.svelte mirroring AddSubjectPanel's shape ("System programmes" / "Custom programme," after removing initial "GES"-specific wording from both this panel and, on a follow-up sweep, the grading-scale name placeholders too — grading scales are school-defined, not tied to a specific national standard; subjects already had no such wording). New tests cover adopt / re-adopt-409 / cross-school-independent-adoption (the literal "school-a offers only Business, school-b offers everything" scenario) and the read-only-catalogue guarantee. Verified live: seeded a fresh global catalogue entry, adopted it, confirmed it disappeared from the catalogue and appeared in the school's own list, confirmed re-adopt 409s, confirmed it was immediately usable on a new class, cleaned up all test data afterward. 356 backend tests passing, svelte-check 0 errors (346 warnings, all pre-existing @apply/a11y pattern). Commits ec97559, 0969a48, 5c95c34; all six commits from this review (59f492c through 5c95c34) pushed to origin/main.
- 12ag: Nullable-`school_id` "shared template" audit (follow-up to 12ad's grading-scale fix — same bug shape checked across every other table with `school_id=NULL` used for system-wide defaults: StaffPosition/StaffCategory/StaffRank were fine; SubjectCatalogue and SHSProgramme were not). Both were only ever seeded by a second, completely orphaned script, `scripts/seed_ges_data.py` — not in CI, not in CLAUDE.md, not imported anywhere. Live-DB check confirmed the damage was real, not theoretical: the SHS programme catalogue had zero rows on the shared dev stack, meaning the phase-12y "adopt a programme" feature had nothing to show any new SHS school. Fixed at the root the same way as 12ad: folded `GES_PROGRAMMES`/`GES_SUBJECTS` seeding into `seed_reference_data.py` (the one script CI and every doc already point to) and deleted `seed_ges_data.py` outright — there is now only one seed script, ever, so this "forgot the second script" failure mode can't recur. While merging, split the file's ~500 lines of static data tables (regions/districts/holidays/positions/categories/ranks/grading-scale/programmes/subjects) out into a new `scripts/reference_data.py`, keeping `seed_reference_data.py` itself as pure `seed()` orchestration at 212 lines (it had already drifted to 621 lines of pre-existing debt, over the 300-line cap, before this session ever touched it — the data-table file itself is still ~540 lines, since a single flat reference table like 212 GES rank titles has no meaningful seam to split further without hurting readability for no real gain; noted here as a deliberate, acknowledged exception to the cap rather than silently leaving it undocumented). Verified live: ran the merged script against the real dev DB — idempotent (0 new for programmes/subjects, since the audit's own investigation had already seeded them moments earlier), full 388-test backend suite green both before and after the data-table split. No test imports the seed script directly (only docstring/comment mentions in fixtures assuming it has been run), so no test changes needed. No CI or CLAUDE.md "How to run" changes needed — both already only ever referenced `seed_reference_data.py`.
- 12ah: Fees module review (asked for directly, next module after Attendance/Academic in this session's rotation) — 2 findings, both fixed. (1) `record_payment()`/`apply_discount()` (services/fees_payment.py) accepted `student_id` as a free-standing field in the request body, entirely decoupled from `fee_record_id` — neither the router nor the service ever cross-checked the two matched. Since the StudentFeeSummary-refresh DB trigger (migration e5f0a2d91c74) keys off the payment/discount row's own `student_id` column (not the fee record's real owner), a mismatched value would misattribute the payment: inflate the wrong student's paid total (fabricating a payment they never made) while the real debtor's fee record stayed outstanding and invisible to their own `list_payments`. No test exercised this and the live frontend always happened to send matching values, but nothing on the server actually enforced it. Fixed by deriving `student_id` from `rec.student_id` server-side in both functions and dropping the redundant field from `FeePaymentCreate`/`FeeDiscountCreate` entirely (no backwards-compat shim — removed from both frontend API client functions and all three call sites: PaymentModal.svelte/DiscountModal.svelte's `studentId` prop, and FeesTab.svelte's two inline mutations). (2) `create_fee_structure()` never verified `applies_to_class_id`/`applies_to_programme_id` belonged to the calling school — same shape as the `create_class`/`update_class` ownership gap fixed in the Academic review (12v). In practice a cross-school id just silently matched zero students at bulk-assign time rather than leaking data, but it broke the "404/422 on cross-tenant or bogus FK" convention used everywhere else — added the same Class/SHSProgramme ownership checks (404 for class, 422 for programme, matching academic_class.py's existing convention). Checked a third candidate (`bulk_assign_fees` filtering only `StudentClassAssignment.is_active`, not `Student.is_active` directly) and confirmed via `student_lifecycle.py` it's not a bug — both are deactivated in lockstep on withdrawal. 4 new backend tests (2 regression tests proving a caller-supplied `student_id` is ignored in favour of the fee record's real owner, 2 for the cross-school-class/bogus-programme rejection); full 400+ backend suite green, svelte-check 0 new errors. Verified live against the real dev DB: posted a payment for a real demo student (Arif Latif) with a deliberately bogus `student_id` in the body, confirmed the response and the resulting `StudentFeeSummary` update both resolved to the fee record's true owner (balance 600→550) with zero row created under the bogus id, then cleaned up the test payment (direct delete, trigger restored the summary to 600/0/600).
- 12ai: Housing module review (last of the originally-flagged "critical feature" modules — Attendance/Academic/Fees done). User flagged upfront that housing only matters for boarding schools, which shaped the review: checked whether the module actually tracks that distinction correctly rather than assuming every school uses it. 3 findings, all fixed. (1) The "Housing" sidebar/bottom-nav link was gated on `school_type` (`schoolTypes: ['SHS', 'TECHNICAL', 'VOCATIONAL']` — the latter two don't even exist in the backend's `SchoolType` enum yet), but the actual gate every `/housing/*` endpoint enforces is a separate `School.has_boarding` flag, completely independent of school type. Net effect: a BASIC school with boarding enabled (a real scenario — some GES basic/JHS schools board students) never saw the Housing link at all despite the backend fully supporting them, while an SHS day-school with boarding off saw the link and landed on an existing "enable boarding?" prompt (already handled gracefully, so not itself urgent). Fixed by adding a new `requiresBoarding` nav-item flag (nav.ts) alongside the existing `roles`/`schoolTypes`/`classTeacherOnly` gates, resolved in Sidebar.svelte/BottomNav.svelte via a `getMySchool()` query (`['my-school']` — same cache key the /housing page itself already uses, so no duplicate fetch) checked against `has_boarding` directly instead of school type. (2) `assign_student()` (services/housing.py) never enforced `House.gender` at all, despite the model's own docstring stating "Gender determines which students can be assigned" — a female student could be placed in an all-male house with zero validation. Fixed with a check against the student's own `Gender` (422 if unset on a single-gender house, 422 on mismatch; MIXED houses unrestricted). (3) Assigning a student to a house never touched `Student.is_boarding` — the same flag Fees' `boarding_only` fee structures (12ah) key off — so a student could be living in a real dorm room while remaining structurally invisible to boarding fees, a quiet revenue leak connecting straight back to the Fees review. Fixed: `assign_student` now sets `is_boarding = True` if not already (vacating deliberately does not reverse it — that's a separate administrative call, not an automatic inference). Split services/housing.py (pushed to 346 lines by these fixes) into `housing_assignment.py` (the four StudentHouseAssignment functions: assign/vacate/get-current/list-house-students) and housing.py (House/Room/HouseMaster CRUD only, back to 213 lines) — router updated to import from both. 6 new backend tests (gender-mismatch rejected, missing-gender rejected, MIXED-house accepts any gender, is_boarding flips true on assignment), all housing tests + full backend suite green, svelte-check 0 new errors. Verified live against the real dev SHS demo school (has_boarding=True): created a throwaway MALE house + FEMALE student, confirmed 422 on the cross-gender assignment; created a matching MALE student, confirmed the assignment succeeded and `is_boarding` flipped false→true; cleaned up all test data afterward.
- 12aj: SMS Notifications module review (last of the modules picked from the post-critical-four rotation — Documents/SMS/Sync remained; SMS chosen since guardians depend on it in real time for fee receipts, absence alerts, and report-card notices). 3 findings, all fixed, plus a small dead-code cleanup. (1) `POST /sms/configs` accepted a caller-supplied `is_active` field and applied it directly — unlike `EmailConfigCreate`, which has no such field at all — so a request setting `is_active: true` while a different provider was already active could leave two `SmsConfig` rows active simultaneously; `_get_active_driver()`'s `db.scalar()` would then silently pick whichever one the database happened to return first, with no error and no way to predict which provider was actually in use. Not reachable via the shipped frontend form (which never sends `is_active`), but nothing on the server prevented it either. Fixed by removing `is_active` from `SmsConfigCreate` entirely — only `POST /sms/configs/activate` may ever set it now, matching how Email config already worked. (2) Every SMS driver normalizes the phone number internally right before sending (`self._normalize(to)`), but `SmsLog.recipient` was written with whatever raw string was passed in — for every path except `notify_staff_invite` (the one function that happened to normalize before logging). So the one place meant to show "what did we actually send and to whom" usually didn't reflect the real destination number. Fixed by moving normalization into the shared `_deliver()` helper (used by all four automatic notify_* functions) and routing `notify_staff_invite`, `notify_portal_access`, and `send_manual` through it too — one normalize-send-log choke point instead of four separately-inlined ones, closing the gap for every path at once. (3) AfricasTalking/Hubtel/Twilio all require `api_secret` to function at all (documented in sms_driver.py's own comments), but nothing enforced this — a provider missing it could be saved and even activated, with the only sign of trouble being FAILED rows quietly accumulating in `/sms/logs` after guardians stopped getting messages. Fixed by checking `PROVIDERS_REQUIRING_SECRET` (new constant in sms_driver.py) inside `activate_sms_provider()` — 422 if the secret is missing, activation blocked outright, matching the point in the workflow where an incomplete config actually becomes load-bearing (deliberately not blocking the initial *save*, since credentials are often gathered in stages and the docstring already treats "configure" and "activate" as separate steps). Small cleanup: `routers/sms.py::send_manual` built an unused `send_results` list (with a bug in it — put the provider name in the `phone` field) that was immediately discarded and replaced by a correct `paired` list; deleted the dead block. 6 new/updated backend tests (client-supplied `is_active` ignored, activation rejected without required secret, activation allowed for a provider that doesn't need one, log recipient asserted as the normalized number); two pre-existing tests (`test_activate_provider_sets_active`, `test_activate_deactivates_others`) needed an `api_secret` added to their AfricasTalking fixture data to keep passing under the new requirement. Full backend suite green, svelte-check 0 new errors. Verified live against the real dev SHS demo school: saved AfricasTalking without a secret, confirmed activation 422'd; added the secret, activated successfully; attempted to smuggle `is_active: true` while saving a second provider (Arkesel), confirmed it was ignored and AfricasTalking remained the sole active provider; cleaned up both throwaway configs afterward.
- 12ak: Offline Sync module review (last of the post-critical-four rotation), preceded by a web search on outbox-pattern/idempotency best practices per the user's request. The standout finding: two completely independent, hand-rolled implementations of "drain the WriteOutbox" were both wired into the live app at once — `lib/offline/sync.ts::drainOutbox()` (triggered from `(app)/+layout.svelte`'s `initOfflineSync()`) and `lib/sync/drainer.ts::drainWriteOutbox()` (triggered independently from `OfflineBanner.svelte`'s own `online` listener). Both fired on the same native browser `online` event, both read the same pending Dexie items before either could mark them synced, and both POSTed them to `/sync/outbox` — meaning a single reconnect could submit the same offline score twice. Worse: since the conflict-detection rule is "existing.submitted_at > offline_session_started_at", the *first* duplicate's own successful apply would set `submitted_at` to right now — which is by definition after the offline session started — so the *second* duplicate would immediately flag a **phantom conflict against itself**, with nothing anyone else ever touched. The `lib/offline/sync.ts` copy was also independently broken: it tagged anything that wasn't a `'Score'` as `entity_type: 'attendance_record'`, a value the backend schema (`Literal["score"]`) doesn't accept — which would 422 an entire mixed batch, scores included. Consolidated onto one path: `lib/offline/sync.ts` now owns state (`pendingOutboxCount`/`isOnline`/`isSyncing`) and is the only thing that ever triggers a drain, delegating the actual send/mark-synced mechanics to `lib/sync/drainer.ts`; `OfflineBanner.svelte` no longer drains on its own, it just reflects the shared `isSyncing` store and re-checks conflicts once a drain (wherever triggered) finishes. Per the web search's core recommendation, also added real idempotency protection rather than relying on the duplicate-listener fix alone (a genuine network retry could still resubmit even with one drainer): new `client_op_id` field, a UUID generated client-side once at `queueWrite()` time — unlike the Dexie local auto-increment id (unique only within one device, and reusable-in-appearance across two different devices/browsers for the same teacher), this is truly globally unique and safe as a dedup key. New `outbox_processed_item` table (migration f7a8b9c0d1e2, keyed on `(school_id, user_id, client_op_id)`) records the outcome of every processed item; `_sync_score` checks it first and returns the recorded result as a no-op on any resubmission, so a retry can no longer double-write a `ScoreAuditLog` entry or reopen a conflict. Two other findings were presented but were a product-scope question, not a bug — the user chose to remove rather than build them out: (a) `outbox.ts`'s `entity` type and docstring claimed "Score and AttendanceRecord" both go offline, but nothing anywhere ever queued an attendance item and the drainer explicitly filtered them out — dead, misleading scaffolding now deleted (entity is just `'Score'`); (b) the entire structured read-side `OfflineCacheDB` (classes/students/terms/subjects/calendar — `lib/offline/cache.ts`, one whole half of CLAUDE.md's own documented "Dexie: OfflineCache + WriteOutbox" architecture) was fully scaffolded — schema, helpers, docstring describing "syncs on every app foreground" — but never populated or read from anywhere in the app; deleted outright, CLAUDE.md's architecture notes updated to stop claiming it exists. 4 new/updated backend tests (two proving duplicate `client_op_id` submissions are true no-ops — one for the applied path, one for the conflict path, asserting exactly one audit-log/conflict row survives either way — plus two pre-existing test files' outbox payloads updated with the now-required `client_op_id` field). Full backend suite green, svelte-check 0 new errors. Verified live: ran the real `_sync_score()` service function directly against the dev DB twice with the same `client_op_id`, confirmed exactly one `ScoreAuditLog` row and one `OutboxProcessedItem` row resulted (not two), cleaned up all test data afterward.
- 12al: Students module class-teacher/subject-teacher permission scoping, prompted directly by the user flagging that a teacher login could "almost do everything an admin can do" on Students. Confirmed precisely: every single-student endpoint (students_detail.py/students_enrollment.py/students_lifecycle.py) was gated only by the flat `require_permission("students", "edit"/"view")` — no per-student ownership check at all — while `list_students` was already correctly scoped via an inline union query. Any staff holding `students.edit` (the live dev DB confirmed the plain `TEACHER` position grants this by default) could view/edit/transfer/promote/graduate/deactivate *any* student school-wide, not just their own class. Extended the existing `core/teacher_scope.py` pattern (already used for Attendance/Assessments/Report Cards) to Students via new `core/student_scope.py`: `resolve_student_view_scope`/`resolve_class_teacher_scope`/`resolve_subject_teacher_scope`/`can_write_student`, bypassed on `students.delete` — reused rather than adding a new `students.manage` permission, since `students.delete` had no real "delete a student" endpoint behind it and was already the de-facto admin tier (transfer-review queue). Four categories, decided via AskUserQuestion with the user before implementation: (A) pastoral single-student writes (profile/guardians/medical/photo/enroll/transfer-create/class-assignment/term-enrollment) — scoped to the caller's own `ClassTeacher` assignment for the student's current class+year, 404 otherwise, never a silent fallback for a teacher with zero assignments; (B) subject registration + per-subject roster — class teacher OR the specific subject's own `SubjectTeacher`; (C) every remaining single-student GET — parity with `list_students`' existing scope; (D) admin-tier-only regardless of scope — portal-access grant/revoke, the `is_active` field specifically within `PATCH /students/{id}` (rest of that endpoint stays Category A), and promote/repeat/demote/graduate (matching nav.ts's existing admin-only gating of the bulk `/admin/academic/promote`/`/graduation` pages). `students.delete` added to `DEPUTY_HEAD`/`HOD` seed templates so they keep unrestricted student access under the new scoping (a disclosed side effect: they also gain transfer-queue visibility they lacked before). `StudentDetail` gained `can_edit`/`can_manage` (real values only from `get_student`, via a `_to_detail()` default of True elsewhere since a mutation response reaching the caller already proves they could act) — frontend gates Profile/Guardians/Medical/Photo/Enrollment edit controls on `can_edit`, Promote/Repeat/Demote/Graduate/portal-access/Deactivate on `can_manage`, deliberately leaves `SubjectRegistrationPanel.svelte` ungated client-side (a subject teacher can act there even with `can_edit=false`) relying on the backend 404. Found during the fix and had to update to match the new stricter scoping: three pre-existing tests (`test_register_subject_reason_alone_insufficient_without_permission`, `test_bulk_register_reason_alone_insufficient_without_permission`, `test_fee_gate_waiver_ignored_without_fees_manage`) used a `CLASS_TEACHER`-position staff member with zero actual `ClassTeacher` rows to test an unrelated permission edge case, which now correctly 404s on scope before ever reaching the behaviour under test — fixed by giving each a real `ClassTeacher` assignment on the class in question. New `test_student_scope.py` (13 unit tests on the resolvers, mirroring `test_teacher_scope.py`) plus ~15 integration tests across `test_students.py`/`test_student_enrollment.py`/`test_student_lifecycle.py`. Full backend suite: two independent clean runs both showed exactly 14 failures, 100% `429 Too many requests` on `/auth/login` (a pre-existing Redis-backed 10/min rate limit shared across the whole pytest session, which has 60+ login-heavy tests — confirmed pre-existing/unrelated to this change since the *specific* tests failing differed between the two runs while the count stayed ~14, and every targeted file passed cleanly in isolation), zero non-429 failures either run. svelte-check 0 new errors. Verified live against the real dev SHS demo school: two throwaway classes with two different `ClassTeacher`s confirmed cross-class 404 on GET/PATCH; a `SubjectTeacher`-only staff (no `ClassTeacher` row) confirmed view-scope includes their taught class (`can_edit=false`) but PATCH 404s; Category B confirmed both ways (their own subject's registration succeeded, a different subject in the same class 404'd, and a different class's teacher also 404'd on it); `is_active`/grant-portal-access/promotions all 403'd for the class teacher and succeeded for the admin (`students.delete`) token; confirmed `DEPUTY_HEAD`/`HOD` live permissions include `students.delete` after the seed re-run. Zero unexpected errors in `docker compose logs api`; all throwaway students/staff/users/classes cleaned up and confirmed gone afterward.
- 12am: Staff role scoping gaps, follow-up to a full staff-roles/responsibilities spec the user provided (Subject Teacher core duty; Class Teacher/Housemaster/Assistant Head/Headmaster as additional duties). A read-only gap analysis found the spec implies much more than exists today — a whole new Assistant Head/portfolio tier, disciplinary escalation/suspension workflow, Headmaster delegation, consolidated reporting — none of which exist in any form; per the user's explicit choice (AskUserQuestion), this session covered only the three contained scoping gaps, deferring everything else. (1) Behaviour recording was gated too high, not just unscoped: `POST/DELETE /behaviour` required `assessments.approve_scores` (HEAD/DEPUTY_HEAD/HOD/EXAM_OFFICER only) — a Class Teacher couldn't record behaviour at all, contradicting their spec'd duty. Added a new narrow `assessments.record_behaviour` permission (PERMISSION_MATRIX + reference_data.py, granted to CLASS_TEACHER and — mirroring the existing enter_scores/approve_scores pattern — to HEAD/DEPUTY_HEAD/HOD/EXAM_OFFICER too, since `require_permission()` is a flat router-level check with no bypass logic of its own; a senior position needs the narrow action to even reach the service layer, where holding `approve_scores` is what actually makes it unrestricted). `services/behaviour.py` now scopes all three functions (create/list/delete) by reusing `core/teacher_scope.py::resolve_report_card_scope()` directly (identical "ClassTeacher this year, bypassed on approve_scores" semantics report cards already use — no new resolver needed) plus `core/student_scope.py::current_class_assignment()` from last session's fix. Also fixed a latent bug this change would otherwise have introduced: the local `_check_term_lock()` helper only checked for a non-blank override reason, never the caller's permission — safe before only because the entire endpoint required `approve_scores` already; replaced with the shared `check_term_lock_override()` (already used by scoring/assessment/subject-registration) so a Class Teacher hitting a locked term still needs `approve_scores` specifically to push through. Frontend: `BehaviourTab.svelte`'s `canManage` gate was role-based (`admin`/`approver` only, from 12m) — would have hidden the newly-legal action from Class Teachers entirely; now also true when the student's own `can_edit` flag is set (same StudentDetail flag from 12al). (2) No subject-only "Teacher" position existed in `reference_data.py`'s `STAFF_POSITIONS` (only `CLASS_TEACHER`) — a known pre-existing drift where an old migration (`q2r3s4t5u6v7`) created a global `TEACHER` template directly in the DB that this script never learned about. This blocked a genuine Subject Teacher's core duty (self-registering students into their own subject) purely on missing permissions — the Category B scoping from 12al already handles it correctly once a caller holds `students.edit`. Added a proper `TEACHER` position (`students.view/edit`, `academic.view`, `assessments.view/enter_scores`, `reports.view`, `school.view` — deliberately no `students.create`/`attendance.*`/`record_behaviour`, all registrar/Class-Teacher jobs). Because `seed_reference_data.py` only ever adds missing permission rows, this reconciles against the orphaned template without touching whatever broader set individual schools' own per-school forked copies already had (same one-way-additive convention as 12ad/12ag) — confirmed live: the demo SHS school's own `TEACHER` fork still lacks `edit` (customized down before this session, untouched by design), while the shared global template now carries the full declared set. (3) `nav.ts`'s Students entry (`roles: ['teacher','admin','approver']`) didn't hide from subject-only teachers despite the spec saying they don't need direct Students-module access, even though the exact `classTeacherOnly` mechanism already existed (proven on the teacher-facing Report Cards entry). Split into two entries mirroring that Report Cards pattern exactly: one unconditional `roles: ['admin','approver']`, one `roles: ['teacher'], classTeacherOnly: true` — nav-visibility only, not route-level, since the backend was already correctly scoped either way. New `test_behaviour_scope.py` (7 tests: class-teacher own-class create/list/delete allowed, other-class 404, zero-assignment 404, plain-TEACHER 403 without record_behaviour, HOD unrestricted) — caught a real bug during first run: HOD's `test_create_unrestricted_for_approve_scores_holder` 403'd because HOD didn't yet hold the narrow `record_behaviour` action needed to clear the router gate (fixed by the enter_scores/approve_scores-mirroring grant to all four senior positions, described above). Also hit and correctly diagnosed as non-issues during live verification: a stale Redis `perms:<staff_id>` cache (15-minute TTL, only invalidated by the normal PUT /permissions/positions endpoint — the seed script writes the DB directly and doesn't call `invalidate_permissions()`, so a manually-triggered reseed needs a manual `DEL` on affected keys to reflect immediately) and this demo school's "admin" account actually holding a custom `SCHOOL_ADMINISTRATOR` position rather than the seeded `HEAD` template (out of scope, left untouched). Full backend suite: 20 failures, all confirmed via the same signature check as 12al (100% `429` on `/auth/login`, matching the documented [[rate_limiter_flakiness]] memory) — zero non-429 failures, zero regressions. svelte-check 0 new errors. Verified live: real throwaway class-teacher scenario against the SHS demo school (create/list/delete own-class 201/200/204, other-class 404 both ways), confirmed `CLASS_TEACHER`/`HEAD`/`DEPUTY_HEAD`/`HOD`/`EXAM_OFFICER` all carry `assessments.record_behaviour` post-reseed. All throwaway data cleaned up and confirmed gone afterward.
- 12an: Assistant Head/portfolio tier — the "cheap version" the user chose (no new approval workflow, no anomaly flagging, no Headmaster delegation — all deferred, per 12am's gap analysis). Added three new seed positions (`reference_data.py`) rather than a new "portfolio" schema concept, since permissions here are already just curated module.action bundles on a `StaffPosition` — the three portfolios line up naturally onto existing modules: `ASSISTANT_HEAD_ACADEMICS` (HOD's existing set plus `attendance.approve` — the one gap that would otherwise leave attendance oversight scoped to just their own ClassTeacher assignment, since HOD alone only has `attendance.record`), `ASSISTANT_HEAD_ADMINISTRATION` (built fresh — `school.view/edit`, `staff.view/create/edit`, `students.view`, `reports.*`, `documents.*` — deliberately no `school.manage_users`/academic/assessments/attendance/fees/housing, those are HEAD's or another portfolio's job), `ASSISTANT_HEAD_BOARDING` (identical to `HOUSEMASTER`'s set — flagged explicitly in a code comment that Housing has no per-house scoping mechanism at all today, unlike Attendance/Assessments/Students, so a Housemaster is *already* unrestricted across every house in the school; this position is currently functionally identical to Housemaster until Housing gets its own scoping fix, a separate pre-existing gap not addressed here — kept as a distinct named position anyway since the spec asks for an organizational appointment distinct from a single house's Housemaster). "School-wide" Assistant Head (the spec's other option, vs. by-portfolio) is deliberately *not* a fourth position — a staff member holding all three portfolio positions simultaneously gets the union via the existing multi-position resolution, so no new position or schema was needed for that case either. Found and fixed one real bug surfaced while building this: `services/dashboard.py::get_dashboard()`'s role-detection cascade (`school.manage_users`→admin, `fees.collect`→finance, `assessments.approve_scores`→approver, `housing.manage`→housemaster, else→teacher) had no catch-all for senior non-teaching staff holding none of those — `ASSISTANT_HEAD_ADMINISTRATION` would have silently fallen through to the teacher dashboard ("my classes: none"), a visibly broken experience for someone who doesn't teach at all. Added one more check (`staff.edit` → approver view) before the teacher fallback; verified live it doesn't accidentally redirect HEAD/DEPUTY_HEAD since both already match an earlier branch in the cascade. No frontend changes needed — the staff position-assignment UI already fetches positions dynamically from `GET /permissions/positions`, so the three new positions appear in the picker with zero code changes, and nav/dashboard visibility follows automatically from the permission-driven view cascade above. Verified live against the real dev SHS demo school: seeded and confirmed all three positions carry their declared permission sets; created a throwaway `ASSISTANT_HEAD_ADMINISTRATION` staff login and confirmed `GET /dashboard` now correctly returns the `approver` view instead of falling through to `teacher`; zero errors in `docker compose logs api`; all throwaway data cleaned up. Targeted regression tests (`test_dashboard.py`, `test_dashboard_stats.py`, `test_students.py`, `test_behaviour_scope.py`) confirmed clean in isolation — the batched run showed the same pre-existing [[rate_limiter_flakiness]] signature on `test_students.py` alone (many login-heavy tests in one file), not a regression from this change.
- 12ao: Renamed the "Report Cards" nav label/page title to "Reports" everywhere (nav.ts's two entries, the /reports page title and PageHeader, TeacherView's dashboard quick-link) — label-only, no functional change. Prompted by the user pointing out transcript (exists, but only as a student-profile-page button) and a future behaviour report don't conceptually fit under a "Report Cards" umbrella; they chose the minimal rename over consolidating other report types into the page for now.
- 12ap: Permission-editor fork-on-edit fix, surfaced while explaining to the user how `PUT /permissions/positions/{id}` works. Most seeded positions (HEAD, TEACHER, HOD, CLASS_TEACHER, HOUSEMASTER, ...) are shared platform templates (`school_id=NULL`); the endpoint edited them in place with no fork-on-first-edit logic at all — a single school's admin toggling, say, HOD's permissions silently changed what HOD means for *every* school on the platform, the same "shared row mutated by one tenant" bug shape already fixed for Programmes (`adopt_programme`/`update_programme`, 12v/12ag) but never applied here. Fixed in `routers/permissions.py::update_position_permissions`: editing a template now forks a school-owned copy (same code/name, `school_id`=caller's school) instead of mutating the shared row; reuses an existing fork if this school already has one for that code (guards against a stale client re-sending the template's id and creating a duplicate); migrates this school's own already-assigned staff off the template's `staff_member_positions` link onto the fork, since `resolve_permissions()` reads by the *specific* `position_id` a staff member is linked to, not by code — without this, existing staff's permissions wouldn't actually change even after the "fix." Found and fixed a second, closely-related bug while verifying the first: `core/permissions.py::resolve_permissions()`'s auto-derived `CLASS_TEACHER`/`HOUSEMASTER` lookup (`staff don't get manually granted these roles`) matched positions by code with **no `school_id` filter at all** — meaning once any school forked one of those two codes, the union would (a) still include the untouched shared template alongside the fork (a fork could only ever *add* permissions, never actually narrow them, for these two positions specifically) and (b) leak a *different* school's fork into this school's derived staff, since code alone isn't unique across schools. Fixed with the same "school-specific copy wins over template" dedup already used by the list endpoint. New `test_permissions.py` (5 tests: editing a template forks a school copy while the template stays untouched; a second edit reuses the existing fork rather than duplicating it; an already-assigned staff member is migrated onto the fork and immediately reflects the narrowed permissions; two schools forking the same code independently without cross-contamination — the literal "school-a offers only X, school-b offers everything" shape from 12ag's Programme tests, applied here; a `CLASS_TEACHER` fork doesn't leak to a different school's auto-derived staff). Full regression batch (`test_teacher_scope.py`/`test_student_scope.py`/`test_behaviour_scope.py`/`test_staff.py`/`test_auth.py`/`test_students.py`/`test_dashboard*.py`/`test_assessment_scope.py`) green — the one batched run that showed 19 failures was confirmed via grep to be 100% the pre-existing [[rate_limiter_flakiness]] `429` signature (57 hits, exactly 3 per failure), not a regression; isolated re-runs of the same tests passed cleanly. Verified live against the real dev SHS demo school: edited the shared `HOD` template via the real `PUT` endpoint, confirmed a new school-owned fork was created with a different id and the correct narrowed permissions, confirmed the shared template's own 17 permission rows were completely unchanged afterward, confirmed exactly one fork row exists platform-wide (not leaked to any other school), zero errors in `docker compose logs api`; cleaned up the throwaway fork afterward (no real staff had been linked to it, confirmed before deleting).
- 12aq: Teacher dashboard redesign, asked for directly — most teachers using this system in Ghana struggle with web apps/technology in general, and the existing `TeacherView.svelte` packed roughly 10–20 distinct pieces of information into one screen for a single-class teacher (greeting/date, an absent-count pill, an animated attendance-percentage donut chart, present/absent badges, 3 separate `StatCard` tiles, a 4-row task list, up to 8 named absent students), almost all in 11–15px text, with no single clear "what do I do right now." Walked through the design direction with the user via AskUserQuestion first: radical simplification (one large action at a time, everything else quieter, drop the donut chart) over a moderate "same sections but bigger" option; and confirmed scope should include two connected fixes on `/attendance` (the page the dashboard's main CTA hands off to), since a simplified dashboard leading into a fiddly attendance page defeats the point. Rewrote `TeacherView.svelte` around a single computed priority — unmarked attendance (any class) beats pending scores beats a calm "you're all caught up" state, beats a distinct honest "no class assigned, go to Assessments" state for subject-only teachers (deliberately *not* claiming "all caught up" for them, since `dashboard_teacher.py::teacher_view()` only ever computes `pending_score_assessments` from `ClassTeacher`-held classes — a subject-only teacher's real subject-teaching workload is invisible to this endpoint, a pre-existing backend limitation noted but not fixed here since the task was scoped to presentation). Only the priority action gets the large hero-card treatment; a multi-class teacher's already-marked classes collapse into one quiet confirmation line instead of repeating the full card. Dropped the `StatCard` grid and header absent-pill entirely (duplicated what the hero already said); moved the absent-student name list behind a collapse/expand toggle (mirroring the existing `$state`-driven chevron pattern from `TermTimelineRow.svelte`, not native `<details>`, for visual consistency with the rest of the app). No backend changes — `GET /dashboard`'s existing `TeacherDashboard` payload already had everything needed. On `/attendance`: added the same single-class auto-select `$effect` `/assessments` already uses (`assessments/+page.svelte:121-124`) — `/attendance` had never adopted that pattern despite being the more time-sensitive daily task; and bumped `AttendanceStudentRow.svelte`'s Present/Absent/Late/Excused buttons from `h-8 w-8` (32px) to `min-h-[44px] min-w-[44px]`, matching the touch-target convention this app already documents and uses on `/assessments` ("WCAG 2.1 AAA / Apple HIG touch-target minimum, since most users are on a phone") but had never applied to its own most-used-on-a-phone page. `svelte-check` 0 new errors/warnings. Verified live against the real dev SHS demo school with a throwaway class-teacher scenario: confirmed all three hero states render from real API responses in the exact priority order intended (unmarked → marked-but-pending-scores → all-caught-up, the last two constructed by directly inserting an `AttendanceRecord` and creating a real unpublished `Assessment` since "today" happened to be a weekend in the seeded calendar, correctly blocking the real mark-attendance write path — unrelated pre-existing behavior, not a bug); confirmed clean SSR (200) on `/dashboard` and `/attendance` and clean Vite HMR reloads for all three edited files; zero unexpected errors in `docker compose logs api`. All throwaway data (class, student, staff, login, class-teacher assignment, class-subject, assessment, attendance record) cleaned up and confirmed gone afterward.

  Follow-up in the same session ("Doesn't look nice, try again"): the hero cards used a bespoke `border-2` brand-colored border with a `color-mix()`-tinted background — a heavier, more saturated treatment than any other card in the app, which reserves brand color for buttons/text/small accents and uses a plain `border border-[var(--border)] bg-[var(--card)]` + `box-shadow: var(--shadow-sm)` for card elevation (confirmed against `AdminView.svelte`/`StatCard.svelte`). Fixed by switching the hero and "all caught up" cards to that same neutral-card-plus-shadow treatment. Also widened the page's `max-w-xl` (576px) wrapper to `max-w-2xl` (672px), matching `/sync`'s established precedent for a narrow single-column content page — the tighter width read as sparse on desktop. A second pass ("call to action buttons are too big") found the hero CTA buttons at `py-4 text-lg font-bold` with a `h-6 w-6` icon dwarfed every other primary button in the app, which converge on `min-h-[44px] px-4 py-2 text-sm font-semibold` (`assessments/+page.svelte`); resized to `py-3 text-base font-semibold` with a `h-5 w-5` icon — still above the 44px touch-target minimum, closer to the app's real button scale. `svelte-check` 0 new errors both times.
- 12ar: Dashboard/attendance UX research pass, asked for directly ("research online for better dashboard and attendance page designs"). Web research on teacher-dashboard and attendance-marking UX converged on patterns already validated in 12aq (single-priority-action dashboard, progressive disclosure) plus one clear gap: real attendance systems (Zoho People's "Present by Default," an ERPNext/Frappe HR exception-based-attendance proposal, multiple case studies converging on "mark a class in under 30 seconds") default every student to Present and have the marker record only exceptions, whereas `/attendance` required an explicit "Mark all present" tap or individually tapping every student, with an interrupting warning dialog if anyone was left blank at Save time. Walked findings past the user via AskUserQuestion before implementing. Fixed in `attendance/+page.svelte`: the roster-init `$effect` now defaults any student without an existing record to `PRESENT` the moment the roster loads, so the teacher only taps the few who are Absent/Late/Excused; `markMut`'s payload coerces a toggled-off (`''`) status back to `PRESENT` at submit time (a toggle-off is "no exception," not "unrecorded," under this model) so there's nothing left to warn about — deleted `markAllPresent`/`doMarkAllPresent`/`confirmMarkAll`/`markRemainingPresent`/`showUnmarkedWarning`/`unmarkedCount` and their markup entirely, replaced with a one-line plain-language caption ("everyone starts Present — tap a student to change..."). Also fixed two smaller findings surfaced by the same research: (1) Nielsen Norman Group specifically flags bare hamburger icons as unclear for low-digital-literacy users — found `TopBar.svelte` had exactly that on mobile (`lg:hidden`, `aria-label` only, no visible text), entirely redundant with `BottomNav.svelte`'s already-labeled "More" button which opens the identical sidebar; removed the `TopBar` hamburger and its now-unused `toggleSidebar` prop (`+layout.svelte`) so there's one clear labeled way to reach the sidebar on mobile, not two. (2) `AttendanceStudentRow.svelte`'s P/A/L/E status buttons relied on a hover `title` tooltip for what each letter means, which doesn't work on touch — added a persistent color-coded legend line above the student list on `/attendance` (kept the single-letter buttons compact rather than spelling out full words, which wouldn't fit four across on a phone). `svelte-check` 0 new errors. Verified live against the real dev SHS demo school: replayed the new frontend's exact submit payload (`status: markInputs[s.id] || 'PRESENT'`) through the real `POST /attendance/mark` endpoint for a real class+student with no prior record, confirmed it saved as PRESENT, zero errors in `docker compose logs api`; cleaned up the created record afterward (no DELETE endpoint exists for attendance records, so this was a direct DB delete, confirmed gone by re-querying).
- 12as: Subject registration blocked on a non-current term, asked for directly ("Subject registration should not be possible on an inactive term/semester"). `register_subjects`/`delete_subject_registration` (`services/student_subject_registration.py`) already gated on `AcademicTerm.results_locked` (12aa) but had no awareness at all of `AcademicTerm.is_current` — a class/subject teacher could register or remove a student's subjects against any term regardless of whether it was the one currently running, unlike Attendance and Scoring, which already restrict a scoped (non-`assessments.approve_scores`) caller to the current term via `core/teacher_scope.py::enforce_current_term_for_attendance`/`enforce_current_term_for_scoring`. Added the third sibling, `enforce_current_term_for_subject_registration`, same shape and same bypass permission (`assessments.approve_scores` — already the exact permission that governs this registration flow's `results_locked` override, so a senior staff member backfilling historical data isn't newly blocked). Deliberately a hard 422 block, not an override-with-reason like `results_locked`: there's no "unlock" for a term simply not being current, so unlike the audited exception path for finalized results, this is just the wrong place to be touching a student's curriculum at all for an ordinary teacher. Wired into both `register_subjects` and `delete_subject_registration`; `class_subject_registration.py`'s bulk/class-wide paths call through the same two functions so the guard applies there for free, no separate wiring needed. Frontend needed no changes — `SubjectRegistrationPanel.svelte`'s existing `onError` handlers already surface a plain 422's `detail` text inline (only a 423 triggers the separate `OverrideReasonModal` flow), so the new backend message ("Subjects can only be registered or removed for the current term.") surfaces correctly with zero frontend code changes. 4 new tests (`test_student_enrollment.py` — blocked/allowed pairs for both register and delete, mirroring the existing `results_locked` test shape); full targeted run (`test_student_enrollment.py`/`test_subject_bulk_registration.py`/`test_subject_registration_audit.py`/`test_students.py`) showed the same pre-existing [[rate_limiter_flakiness]] `429` signature on 2 unrelated tests, both confirmed passing in isolation — zero regressions. Verified live against the real dev SHS demo school: found a real non-current term already sitting on the school (`Semester 1`, `is_current=False`) with a real student's `TermEnrollment` against it; as the real class teacher (no `approve_scores`), confirmed registering a subject against that enrollment now 422s with the exact message above, then confirmed registering a different subject against the same student's *current*-term enrollment still succeeds (201) — cleaned up the one throwaway registration and its audit-log row (`SET NULL` on delete, so the audit row survived with a null `registration_id` and needed its own explicit cleanup) afterward, confirmed both gone by re-querying; zero errors in `docker compose logs api`.
- 12at: Subject registration requires an assigned subject teacher. Follow-up to 12as, prompted by the user pointing out a second gap on the same Enrollment tab screen while re-testing that fix: "assigning a subject to a student without the associated teacher makes no sense, who will teach the student?" Investigated both parts of the report. (1) The non-current-term block from 12as was working as designed — the account the user tested with (`admin@shs.school`, a `SCHOOL_ADMINISTRATOR` position holding `assessments.approve_scores`) is deliberately unrestricted, same bypass as Attendance/Scoring. Confirmed via `AskUserQuestion` the senior-staff override should stay (matches Attendance/Scoring precedent) rather than becoming a hard block for everyone — no code change needed, just confirmation the design was intentional. (2) The teacher gap was real: `register_subjects` only ever checked `class_subject_exists` (the subject is on the class's curriculum) — nothing checked whether a `SubjectTeacher` was actually assigned to teach it. The two are set up as separate steps (`ClassSubject` via Academic Structure, `SubjectTeacher` via `SubjectsTab.svelte`), so a subject could sit on the curriculum with nobody teaching it and still be registered to a student. Confirmed via a second `AskUserQuestion` this should require a `SubjectTeacher` specifically, not just "the class has *a* class teacher" (the alternative that would've covered Basic/JHS's one-teacher-does-everything model) — the strict reading of "who will teach the student" won out. Added `services/subject_roster.py::subject_teacher_assigned()` (mirrors `class_subject_exists`'s shape exactly) and wired it into `register_subjects`'s per-item loop, right after the existing `class_subject_exists` check — raises 422 naming the subject ("has no teacher assigned for this class yet"). No bypass permission, unlike the term checks — this isn't a permission question, a subject genuinely has nobody to teach it regardless of who's asking, so even `assessments.approve_scores` holders are blocked until a `SubjectTeacher` row exists. `class_subject_registration.py`'s bulk/roster paths inherit the guard for free (same `register_subjects` call-through as 12as); worth noting `bulk_register_core_subjects` sends every core subject for a class in one `register_subjects` call per student, so a single un-taught core subject now blocks the *whole* bulk action for that class until a teacher is assigned — judged correct, not a regression, since bulk-registering into a subject nobody teaches is the exact failure mode being closed. Frontend: `SubjectRegistrationPanel.svelte` already computed `subjectsWithTeacher` and labeled untaught subjects "(no teacher assigned)" in the dropdown, but the option was still selectable — comment said "informational only, never blocks registration," now inaccurate. Changed the option to `disabled` so the block is visible before Add is even clicked instead of surfacing only as a 422 afterward; updated the stale comment. `SubjectRosterPanel.svelte`/`BulkRegisterCoreSubjectsButton.svelte` (admin-side, opened from `SubjectsTab.svelte` where the teacher-assignment state is already visible right there) left as-is — their existing `onError` handlers already surface the plain-422 detail text via `toast.error`, judged sufficient for that more teacher-assignment-aware context. Retrofitting existing tests turned out to be most of this session's effort: `register_subjects` had ~27 call sites across `test_student_enrollment.py`/`test_subject_bulk_registration.py`/`test_subject_registration_audit.py` that pre-dated this requirement, none creating a `SubjectTeacher`; fixed by adding a `SubjectTeacher` at each shared fixture (`math_subject`/`french_elective` in the bulk file, the single `subject` fixture in the audit file, a new `_assign_subject_teacher()`/`_assign_teacher()` helper reused across ad hoc per-test `Subject`s in the enrollment file) rather than touching every call site individually — tests asserting an earlier-firing rejection (404/423 for scope/term-lock/deactivated-enrollment) were correctly unaffected since those checks still fire before this new one. 2 new tests (`test_register_subject_rejected_when_no_teacher_assigned`, `test_register_subject_allowed_once_teacher_assigned`) plus the ~24 retrofitted call sites across the three files, all passing. Full-suite run showed the same pre-existing [[rate_limiter_flakiness]] signature (10 failures, 100% `429` on `/auth/login` — this time self-inflicted by rapid repeated full-suite/file-level reruns during verification, not a fresh regression); each of the 10 confirmed passing when re-run individually with pacing to stay under the 10/min window. `svelte-check` 0 new errors. Verified live against the real dev SHS demo school: created a throwaway subject, attached it to a real class with no `SubjectTeacher`, confirmed registering a real student for it 422'd with the exact message, assigned a real teacher via the real endpoint, confirmed registration then succeeded (201) — cleaned up the throwaway subject, `ClassSubject`, `SubjectTeacher`, registration, and its audit-log row afterward, confirmed all gone by re-querying; zero errors in `docker compose logs api`.
- 12au: Housing per-house scoping, closing a gap flagged (but deliberately not fixed) as far back as 12ai/12an — any `housing.manage`/`assign`/`view` holder, Housemaster included, was unrestricted across every house in the school, since HOUSEMASTER and `ASSISTANT_HEAD_BOARDING` hold the exact same permission triple as HEAD itself (no permission-level distinction exists within the module at all). Two ad hoc scoping checks already existed inline (`services/housing.py::list_my_houses`, `services/housing_events.py::create_exeat`) but ~17 other endpoints had none. Confirmed the bypass rule via `AskUserQuestion` before implementing: "admin manages houses, housemasters see only their own house" — i.e. keep the existing convention (zero active `HouseMaster` rows this year = unrestricted) rather than switching to Students' stricter "empty means empty, bypass on an explicit permission" pattern; `HouseMaster` is an appointment only real housemasters ever get, so "zero rows" reliably signals "not a housemaster," unlike Students where a `ClassTeacher`-less teacher is a normal, expected state. New `core/housing_scope.py`: `resolve_house_scope()`/`assert_house_in_scope()` mirror `core/student_scope.py`'s shape exactly; `current_year_id()` resolves "current year" for the many endpoints with no year context of their own (House/Room are permanent structures, not per-year rows); `assert_unrestricted()` is new relative to the Students/Attendance/Scoring precedent — needed because Housing has no distinguishing permission the way `students.delete`/`assessments.approve_scores` gate those modules' admin tiers, so "creating a new house" and "appointing a housemaster" (both administrative decisions, not day-to-day house management) are gated on the caller resolving to `None` scope directly rather than on a stronger permission. Wired into all ~20 endpoints across `services/housing.py`/`housing_assignment.py`/`housing_events.py`; `list_houses`/`list_my_houses` collapsed into one scoped query (`/houses/mine` kept as a route purely for the housemaster-facing frontend's clearer naming, not a different access level); `create_exeat`'s existing ad hoc check now calls the shared resolver instead of duplicating the query; `review_exeat`/`record_return`/`list_student_exeats` gained a new `_assert_student_in_scope()` helper (resolves the student's current house assignment, 404s using the same message as "doesn't exist" rather than revealing it belongs to a different house) since exeats scope by student, not directly by house. `get_student_current_assignment` returns `None` rather than leaking "assigned elsewhere" to an out-of-scope caller, matching Students' no-leak convention. New `test_housing_scope.py` (15 tests: 9 direct resolver unit tests mirroring `test_student_scope.py`'s style — superadmin/no-row/no-year/scoped/inactive-row/wrong-year-row all resolving correctly, `assert_house_in_scope`/`assert_unrestricted` raising the right status codes — plus 6 HTTP-level integration tests covering list/get/update/create/assign-housemaster/rooms/house-students/assign-student/vacate); all 33 pre-existing `test_housing.py`/`test_housing_events.py` tests passed completely unchanged (they only ever use the superadmin `auth` fixture, which was always unrestricted before and after). Full targeted regression run showed the same pre-existing [[rate_limiter_flakiness]] `429` signature on unrelated dashboard/student tests, zero relation to this change. Verified live against the real dev SHS demo school: invited a real throwaway staff login, assigned them `HOUSEMASTER` of one real house (`Bashirudeen`), confirmed both `/houses` and `/houses/mine` returned only that house, confirmed `GET`/`PATCH` on the school's other real house 404'd, confirmed `POST /housing/houses` and assigning a housemaster to the other house both 403'd with the admin-tier message, confirmed the real admin login still saw both houses throughout — cleaned up the throwaway `HouseMaster` row, `User`, `UserInvitation`, position link, and `StaffMember` afterward, confirmed all gone by re-querying; zero errors in `docker compose logs api`. `svelte-check` 0 new errors — no frontend changes needed, the existing `listHouses()`/`listMyHouses()` call sites (admin pages, student profile, staff responsibilities) now transparently receive a correctly-scoped list from the same endpoints.
- 12av: Promotion programme/stream continuity — "1BUS-A should promote to 2BUS-A, not 2ART-A; 1BUS-B to 2BUS-B... you cannot promote students from Business class to Science class." `services/promotion.py::process_bulk_promotion()` took the target `class_id` completely at face value with zero validation against the student's actual current class — proven by an existing (now-corrected) passing test that submitted `graduation_type: "DEMOTED"` with a target class a year *ahead* and got a clean 201, meaning not even PROMOTED/REPEATED/DEMOTED direction was checked, let alone programme/stream. Confirmed two strictness decisions via `AskUserQuestion` before implementing: programme mismatch (Business → Science) and stream mismatch (1BUS-A → 2BUS-B) are both blocked by default with the same override-and-reason escape hatch, not a hard wall — a student genuinely switching programmes is rare but real. New `backend/app/services/class_progression.py`: `expected_graduation_type()` compares a composite `(level_rank, year_group)` ordinal, not raw `year_group` — Ghana Basic schools ladder Creche→Nursery→KG→Basic 1..9, so "KG 2 → Basic 1" is a genuine promotion with a *decreasing* year_group, and a naive check would reject the single most common Basic-school promotion in the country (`CLASS_LEVEL_ORDER` moved here from a private copy in `student_list.py`, which now imports the shared one). `programme_mismatch()`/`stream_mismatch()` are None-safe — Python's `None != None` is `False`, so two Basic-school classes (both `programme_id=None`) correctly compare as a match, the exact trap this fix exists to avoid getting backwards. `validate_promotion_batch()` runs for the whole batch up front, before any write (mirrors `student_subject_registration.py`'s "validate every item up front" precedent) — hard errors (no active class to promote from, wrong direction) are 422 and deliberately **not overridable** (they're derived facts, not judgment calls: there's no legitimate "this is a demotion into a higher year"); an overridable programme/stream mismatch is 423 without a non-blank `override_reason` on the new `BulkPromoteRequest` field (batch-level, not per-record, since every frontend flow submits one target class for a whole batch — a mismatch is by construction the same mismatch across it). Students who already have a `GraduationRecord` this year are exempted from validation entirely, preserving the endpoint's existing idempotency contract (re-posting an already-processed batch can't start failing under stricter new rules). New migration `e6f7a8b9c0d1` adds `GraduationRecord.override_reason`/`source_class_id` (dedicated columns, not reusing `.notes` — reusing would be forgeable, unqueryable, and breaks this codebase's own unbroken precedent of a dedicated `reason` column for every other override: `ScoreAuditLog`, `BehaviourAuditLog`, `AssessmentAuditLog`, `SubjectRegistrationAuditLog`, `TermEnrollment.fee_waiver_reason`); `source_class_id` also closes a structural gap — `GraduationRecord` previously stored neither class, making a promotion unauditable after the fact. Also added an `exclude_academic_year_id` kwarg to `student_display.py::_active_class_assignment_subquery()` so resolving a student's *source* class for validation can't be fooled by an assignment already manually created for the *target* year. Frontend: three components (`admin/academic/promote/+page.svelte`, `students/[id]/ClassActionPanel.svelte`, `admin/academic/classes/[id]/PromoteSection.svelte`) each duplicated an identical client-side "suggested class" snippet that only pre-filled a dropdown default — the dropdown itself always listed every class in the school unfiltered, and the backend never checked anything, so this was purely cosmetic. Consolidated into new `frontend/src/lib/classProgression.ts` (mirrors the backend ladder exactly) and a new shared `TargetClassPicker.svelte` component: the dropdown now defaults to only programme/stream/direction-compatible classes (usually resolving to exactly one), a "Show all classes" checkbox is the deliberate friction replacing the old always-unfiltered list, and an inline amber warning names the mismatch before submit — the actual override-reason prompt always comes from the server's 423 via the existing `OverrideReasonModal.svelte` (reused as-is, already used by attendance/scoring/subject-registration term-lock overrides), never client-side detection, so client and server can't drift. New shared `frontend/src/lib/apiError.ts` (`detailOf`/`isLocked`) for the three new call sites, replacing what would otherwise have been a 4th/5th/6th copy of that exact pair of helpers. Found and fixed a bug in my own first draft of `compatibleClasses()` before it shipped: it unconditionally excluded the source class from the compatible list, which is correct for promote/demote but breaks repeat — Class isn't year-scoped in this data model, so "repeating" a year means a new `StudentClassAssignment` against the very same `class_id`, making the source class the *only* valid repeat target. Two of the three components (`ClassActionPanel.svelte` at 289 lines, `admin/academic/promote/+page.svelte` at 296) were already at this project's 300-line cap before this change even added anything — extracted `TransferOutPanel.svelte` (the unrelated transfer-out half `ClassActionPanel.svelte` already documented as a separate concern) and `PromoteStudentList.svelte` (the student-selection table) respectively, landing both at ~220-265 lines after the promotion-picker wiring was added on top. New `backend/app/tests/test_promotion_validation.py` (23 tests: 5 direct pure-function unit tests on the ladder/mismatch helpers mirroring `test_housing_scope.py`'s split style, plus 18 HTTP-level integration tests — matching programme+stream needs no reason, programme/stream mismatch blocked/allowed-with-reason, **Basic-school both-null programme is a match**, **KG 2 → Basic 1 succeeds despite the lower year_group**, wrong-direction PROMOTED/DEMOTED rejected even *with* a reason supplied, REPEATED into same vs. different class, no active class assignment, **no partial writes on a mixed batch** — one bad record blocks the whole batch, zero rows written — **already-processed records stay exempt from stricter reprocessing**, whitespace-only reason treated as absent, cross-school target 404s). Existing `test_student_lifecycle.py::test_bulk_promote_demoted_type_accepted` — whose premise *was* the bug — deleted there and re-added corrected in the new file; grep-confirmed the other 8 pre-existing `/students/promotions/bulk` tests needed zero fixture changes (their shared fixtures already have matching `programme_id=NULL`/`stream='A'` and a forward ordinal), a far smaller retrofit than 12at's ~27-call-site subject-registration one. `svelte-check` 0 new errors. Verified live against the real dev SHS demo school with four throwaway students: a real Agricultural-Science-to-Business promotion 423'd naming both real programmes, succeeded with a reason and the DB confirmed `override_reason`/`source_class_id` landed correctly, a wrong-direction `DEMOTED` attempt 422'd even with a reason supplied (and named the correct fix — "set graduation_type to REPEATED"), and a same-class `REPEATED` succeeded cleanly with no reason needed; all four throwaway students and their records/assignments cleaned up afterward, confirmed gone by re-querying; zero errors in `docker compose logs api`.
- 12aw: Assessments module — mobile usability + production-readiness security pass, asked for directly. Dispatched two parallel research agents (mobile UX read of the frontend, security audit of the backend) rather than doing it serially, since the two reviews touch almost entirely disjoint files. Security audit found scoping/tenant-isolation already solid (every object lookup and its FK references are correctly school-scoped, no raw SQL, no mass-assignment gaps) — three real gaps, all fixed: (1) `Score.assessment_id`/`ScoreAuditLog.score_id` were CASCADE all the way down, so deleting an assessment silently erased every `ScoreAuditLog` row for its scores — the exact "who changed what, when, why" trail the table exists to preserve, and the one place in this codebase that hadn't already gotten the `SET NULL` treatment `AssessmentAuditLog`/`BehaviourAuditLog` use for the same reason. Fixed at the model level (`score_id` now nullable, `ondelete="SET NULL"`, migration `b7c8d9e0f1a2`) — also had to strip the `Score.audit_logs`/`ScoreAuditLog.score` ORM relationships entirely (both carried `cascade="all, delete-orphan"`, which deletes children in Python before the DB's own `ON DELETE SET NULL` ever runs; confirmed via grep that no code anywhere navigated that relationship, and `AssessmentAuditLog`/`BehaviourAuditLog` already use the same no-relationship, FK-only pattern for the same reason). Confirmed via `AskUserQuestion` before implementing (vs. the alternative of only logging a deletion summary). (2) `create_assessment` never checked `AcademicTerm.results_locked`, unlike every sibling mutation (update/delete/submit/approve/behaviour/subject-registration) — a teacher with only `enter_scores` could create brand-new assessments in a "frozen" term with no reason required and no audit trail. Fixed by reusing `check_term_lock_override()` the same way `update_assessment` does; `AssessmentCreate` gained `override_reason`, and a locked-term override now writes an `AssessmentAuditLog` entry (creation had none before at all). (3) `Assessment.max_score` had no positivity check — a 0/negative value would self-lock scoring for that assessment (every real score fails the `0..max_score` range check), the same "trust unnormalized client numeric input" class of bug as 12u's percentage-normalization fix. Added a `field_validator` on both `AssessmentCreate`/`AssessmentUpdate`. Mobile UX audit's top finding: `ScoreTable.svelte` — the daily score-entry screen — was the one place in this module that hadn't adopted the touch-target/input conventions the rest of the app already uses (Attendance's `AttendanceStudentRow.svelte`, the dashboard hero-card pattern). Fixed: score input and Save button now meet the 44px minimum; added `inputmode="decimal"` (numeric keypad) and `enterkeyhint="next"`; max score is now shown in the column header on every breakpoint instead of being hidden specifically on mobile (`hidden sm:inline` removed) — backwards from what a phone-first teacher needs; the entry-flow hint text no longer describes a desktop-only interaction. Added a genuine unsaved-scores navigation guard (`ScoreTable` reports `hasUnsaved` up via a new `onUnsavedChange` callback; the parent page wires `beforeNavigate` + a `ConfirmModal` for in-app navigation and a native `beforeunload` prompt for tab-close/refresh) — the state already existed and was displayed, but nothing stopped a teacher from losing unsaved entries by tapping away. Icon-only Edit/Delete buttons on the assessment detail toolbar (`AssessmentActionsBar.svelte`) gained visible text labels, not just a `title` tooltip — a tooltip never shows on a touch screen, so a phone user had no way to know what the icon did before tapping it (the same failure mode already fixed once for `TopBar.svelte`'s hamburger). The three admin-only settings pages (`ScaleCard.svelte`/`TypeRow.svelte`/`GradeBandsPanel.svelte`) got the lighter treatment appropriate to a low-traffic config screen — enlarged to a real 44px touch target plus `aria-label`, rather than adding visible text to every icon in an already-dense row. `CreateAssessmentForm.svelte` wired to the same term-lock override flow as create_assessment's new backend check (`OverrideReasonModal`, matching the existing pattern on the assessment detail page). New tests: `test_scoring_lock.py` gained create-assessment blocked/allowed-with-reason pairs and a `test_delete_assessment_preserves_score_audit_log` regression (submits a real score via the API, deletes the assessment, confirms the `ScoreAuditLog` row survives with `score_id=NULL`); `test_assessments.py` gained non-positive-`max_score` rejection tests for both create and update. Targeted regression (`test_scoring_lock.py`/`test_assessments.py`/`test_assessment_scope.py`/`test_sync.py`/`test_sync_resolve.py`/`test_report_cards.py`) all green — the only failures on first run were the pre-existing [[rate_limiter_flakiness]] `429` signature on `test_assessment_scope.py`, confirmed by an isolated rerun passing cleanly. `svelte-check`: 0 new errors (ran inside the frontend container — the host's own `node_modules` was missing `@sveltejs/adapter-node`, unrelated to this change). Deliberately deferred (lower-impact, noted but not fixed): showing an entered/pending-score count on the assessment list cards would need a new backend aggregate field (`AssessmentRead` has no such count today) rather than a pure frontend change.

- 12ax: Fees module — same mobile-usability + security pass as 12aw (Assessments), next module in the post-Assessments rotation the user asked to continue with. Dispatched the same paired research-agent approach. Security audit found tenant isolation solid (12ah's fixes held up under re-verification, no cross-school leaks anywhere) — one real authorization gap and several financial-correctness gaps, all fixed. (1) The four per-student fee read endpoints (`GET /fees/students/{id}/records`, `.../summary`, `GET /fees/payments`, `GET /fees/discounts`) were gated only by flat `fees.view` with zero scoping, unlike every other module (Students/Attendance/Assessments/Housing) that draws this exact line — a `CLASS_TEACHER`/`TEACHER` (both hold `fees.view` but not `fees.collect`/`fees.manage`) could read *any* student's balance, payment history, and discount reasons (e.g. "SCHOLARSHIP", "STAFF_WARD") school-wide, not just their own class. Fixed by wiring the existing `core/student_scope.py::assert_can_view_student()` into all four — it already returns "unrestricted" for `fees.collect`/`fees.manage` holders per its own docstring, so bursars are unaffected; zero permission-matrix changes needed. (2) `FeeDiscountCreate.amount` (the fixed-GHS discount branch) had no positivity check — a negative value flows straight into the `StudentFeeSummary` trigger's `SUM(fd.amount)` and *increases* the computed balance, an unaudited fee hike disguised as a discount row; `percentage` was already correctly bounded, `amount` wasn't. Added the missing `field_validator`. (3) `apply_discount`/`create_instalment_plan` had no `is_waived` check, unlike their sibling `record_payment` — added, mirroring the existing pattern (currently low-impact since nothing in the codebase has a "waive a fee" endpoint yet — `is_waived` is a fully-consulted but never-written flag, flagged as a separate pre-existing gap, not fixed here since it's a missing feature, not a bug). (4) `create_instalment_plan` never validated a plan's total against the fee record's `amount_due` — a schedule could be saved that silently under- or over-covers the real amount owed; added an exact-match check (0.01 tolerance for rounding), and updated `InstalmentModal.svelte`'s client-side submit to catch the same mismatch before round-tripping a 422 (the modal already showed an amber "fee is X" hint for this case but let submission through anyway). (5) `FeePayment`/`FeeInstalmentPlan` were the only fee tables without `TimestampMixin` — `payment_date` is self-reported/unbounded/client-controlled, so there was no way to tell when a payment was actually entered into the system versus what date it claims to be for, the exact question a financial dispute needs answered. Added `TimestampMixin` (migration `c4d5e6f7a8b9`, additive with `server_default=now()`) and exposed `created_at` on `FeePaymentRead`, matching `FeeDiscountRead`'s existing pattern. (6) `get_fee_summary`'s `StudentFeeSummary` query was missing the `school_id` filter every sibling query has (defense-in-depth only — `student_id` was already pre-verified against `school_id` via the `Student` lookup immediately above, not currently exploitable, but broke the established convention). (7) The fee-receipt notification balance was computed as `float` despite both `notify_fee_receipt`/`notify_fee_receipt_email` being typed `Decimal` — fixed to stop converting, cosmetic (notification text only, the stored balance was always `Decimal`-correct via the trigger). Mobile UX audit's top finding mirrored Assessments' `ScoreTable` pattern almost exactly: `FeesTab.svelte`'s Pay/Discount/Instalments buttons (and `FeeLedger.svelte`'s Pay/Discount) were ~24px, well under the 44px minimum — bumped everywhere. No amount input anywhere in the module had `inputmode="decimal"` — added to all four (Payment/Discount/Structure/Instalment). Only `InstalmentModal.svelte` had modal-scroll safety (`max-h-[70vh] overflow-y-auto`, protecting the Save button from a phone's on-screen keyboard) — added the same treatment (`max-h-[90vh]` on a flex-column wrapper, `overflow-y-auto` on the body only, header stays fixed) to `PaymentModal`/`DiscountModal`/`FeeTypeModal`/`FeeStructureModal`. Icon-only modal close buttons relied on a hover `title` alone (invisible on touch) across most of the module — added `aria-label` + real 44px touch targets throughout. The single highest-value fix: `FeesTab.svelte` (the Student Profile's Fees tab — likely the more-used entry point than the standalone `/fees` page, since staff are usually already on the student) had hand-rolled its own complete duplicate of `fees/PaymentModal.svelte`/`DiscountModal.svelte`, which had quietly drifted to be missing the Notes field entirely — `payNotes` was captured in state but the modal never rendered an input for it, so a note typed... never; there was nowhere to type one. Fixed by deleting the duplicate outright and wiring `FeesTab.svelte` to the shared components instead (matching how `InstalmentModal.svelte` was already shared, just not the other two) — one component per concern, not two copies silently drifting apart, and both use sites now inherit every mobile fix made to the shared originals for free. Also added Due/Paid micro-labels to `FeeLedger.svelte`'s ledger rows (previously two unlabeled numbers side by side) and bumped touch targets on `PaymentsTab.svelte`'s secondary Clear/Retry links. New `test_fees_scope.py` (4 tests: `CLASS_TEACHER` 404s on a student outside their class for both summary and payments, succeeds once actually assigned as that student's `ClassTeacher`, `BURSAR` stays unrestricted regardless of class) plus 6 new tests in `test_fees_payment.py` (negative/zero discount amount rejected, discount/instalment-plan blocked on a waived record, instalment total not matching `amount_due` rejected). Full targeted suite (`test_fees.py`/`test_fees_payment.py`/`test_fees_scope.py`) green; a broader regression batch (`test_report_cards.py`/`test_transcript.py`/`test_dashboard.py`/`test_dashboard_stats.py`) showed the same pre-existing [[rate_limiter_flakiness]] `429` signature on 4 dashboard tests, confirmed passing in isolation — zero regressions. `svelte-check` 0 errors (353 warnings, down from 362 — some pre-existing missing-`aria-label` warnings were incidentally fixed along the way), clean HMR reloads, `/fees` SSR 200 live against the running dev stack.

- 12ay: Staff module — same mobile-usability + security pass as 12aw/12ax (Assessments/Fees), continuing the module rotation. Dispatched the same paired research-agent approach. Security audit surfaced something more serious than the prior two passes: **self-escalation and account takeover**, both stemming from `staff.edit` (held by `DEPUTY_HEAD` and `ASSISTANT_HEAD_ADMINISTRATION`, not just `HEAD`) being treated as unconditional full-record write access with no relative-seniority check. (1) `PATCH /staff/{id}` let any `staff.edit` holder change *any* staff member's `position_ids`, including their own — `_assert_positions_owned()` only checked a position belonged to the school, never that it was no more powerful than what the caller already held. A `DEPUTY_HEAD` could `PATCH` themselves onto the `HEAD` position and instantly gain `school.manage_users`/`staff.delete`/`fees.manage`/`housing.manage`. (2) `POST /staff/{id}/reset-password` was gated only by `staff.edit`, no seniority check on the target — the same `DEPUTY_HEAD` could reset the `HEAD`'s password and log in as them directly, full account takeover, not just a permission-set match. Confirmed via `AskUserQuestion` before fixing (two directions: require the stronger `school.manage_users` for both, matching the existing `POST /staff/{id}/invite` precedent exactly — chosen — vs. a more surgical self-target/seniority-comparison rule) since it's a real behavior change removing capability from two roles. Fixed by gating `position_ids` changes and password-reset on `school.manage_users` specifically (profile-field edits stay on `staff.edit`, verified `ProfileTab.svelte`'s edit mutation never sends `position_ids` so ordinary edits are unaffected); both now write to `models.auth.AuditLog` — investigated first and found this generic "immutable record of every write action" table was fully defined but **never once instantiated anywhere in the entire codebase** (every other module built its own typed audit log — `ScoreAuditLog`/`AssessmentAuditLog`/`BehaviourAuditLog`/`SubjectRegistrationAuditLog` — leaving this one dead), so this is its first real use rather than a new table. Frontend: gated the "Authority" position picker + remove-position button in `ResponsibilitiesTab.svelte` and the "Reset password" button in `[id]/+page.svelte` behind `$userRole === 'admin'` (confirmed to be exactly `school.manage_users`, per `services/dashboard.py`'s own role-detection cascade) rather than letting a `DEPUTY_HEAD` tap a control that will now cleanly 403. Everything else audited came back clean: no cross-tenant leaks (every lookup school-scoped, `_assert_positions_owned`/`_assert_rank_visible`/`_get_visible_category` already close the shared-template cross-school gap from 12ap/12ag), no mass-assignment, no injection, photo pipeline reuses the shared validated-image path; `staff.view` being flat/unscoped is by design, not a Fees-shaped gap — confirmed only `HEAD`/`DEPUTY_HEAD`/`HOD`/`ASSISTANT_HEAD_ACADEMICS`/`ASSISTANT_HEAD_ADMINISTRATION` hold any `staff.*` permission at all (`CLASS_TEACHER`/`TEACHER`/`HOUSEMASTER`/`EXAM_OFFICER`/`BURSAR` hold none), so the staff directory is legitimately admin/HR-only. Mobile UX audit's top finding: `StaffPhotoAvatar.svelte`'s upload/remove controls were pure `group-hover` — `opacity-0`/`hidden` with no touch equivalent at all, meaning a phone user literally could not discover or use them; fixed with an always-visible lighter-tint overlay (fully opaque on hover/focus) and an always-shown remove badge (deliberately kept at 32px rather than the module's usual 44px — a full touch target would swallow most of a 64-80px avatar). `PermissionsTab.svelte` (the densest screen, up to 29 rows) and the icon-only Edit/Delete buttons on `PromotionsTab.svelte`/`QualificationsTab.svelte` got the same `flex-wrap` + `min-h-[44px]` + `aria-label` treatment established in the Assessments pass; `min-h-[44px]` added to every plain input/select across the module's six form-bearing files (~28 call sites). `StaffForm.svelte` (the ~10-field "Add staff" drawer) gained an unsaved-changes guard on both the backdrop-click and Cancel/Close paths — the codebase's first *local* unsaved-changes guard (a `ConfirmModal` gated on `hasUnsaved`, distinct from `ScoreTable.svelte`'s SvelteKit-navigation-based guard from 12aw, since this drawer never navigates anywhere). `ProfileTab.svelte`'s emergency-contact removal had zero confirmation (its sibling tabs on the same page already used `ConfirmModal` for their own deletes) — fixed, and while touching it, extracted `EmergencyContactsPanel.svelte` (this pushed the file to 317 lines, over the 300-line cap — pre-existing debt at 314 before this session, not newly introduced) landing both files well under the cap. `StaffImportModal.svelte` gained modal-scroll safety (`max-h-[90vh]` flex-column skeleton, matching `PaymentModal.svelte`'s pattern from 12ax) on both its upload and results phases, plus `aria-label` on both its close buttons. `LeaveTab.svelte`'s Approve/Reject buttons bumped to 44px; its Dates column — previously `hidden sm:table-cell` — is now always shown stacked under the leave type, since date range is exactly what an approver needs before making that specific call. New `_login_as_position()` test helper (mirroring the pattern already used in `test_scoring_lock.py`/`test_behaviour_scope.py`/etc.) plus 7 new tests in `test_staff.py` covering both escalation fixes blocked/allowed, the `AuditLog` rows landing correctly, and ordinary profile edits staying unaffected. Full targeted suite (`test_staff.py`/`test_staff_export.py`/`test_staff_import.py`, 65 tests) green; a broader regression batch (`test_dashboard.py`/`test_dashboard_stats.py`/`test_permissions.py`) showed the same pre-existing [[rate_limiter_flakiness]] `429` signature on one test, confirmed passing in isolation — zero regressions. `svelte-check` 0 errors (351 warnings, down from 353), clean HMR reloads, `/admin/staff` SSR 200 live against the running dev stack.

- 12az: Inactive-class write gap, reported directly ("a class marked as inactive is still allowing us to assign subjects and almost everything an active class can do"). Investigation confirmed `Class.is_active` — the class-as-an-entity retirement flag, distinct from `StudentClassAssignment.is_active`/`TermEnrollment.is_active` (per-student membership) — was checked nowhere in the codebase; every structural write that takes a `class_id` treated a retired class exactly like a live one. New `services/academic_class.py::get_active_class()` (404 if the class doesn't exist/isn't this school's, 422 "This class is inactive." otherwise — same two-step shape already used for cross-tenant ownership checks elsewhere) wired into the seven real gaps found: `assign_subjects` (the literal bug report), `assign_class_teacher`/`assign_subject_teacher` (previously didn't even fetch/validate the `Class` row at all), `create_class_assignment` (a student could be freshly assigned into a retired class), `register_subjects` (via the student's resolved class, not the request body directly), `create_assessment`, and `mark_attendance`. `class_progression.py::validate_promotion_batch`'s target-class query gained an `is_active` filter so a retired class 404s as a promotion target exactly like a nonexistent one; `subject_roster.py::list_my_subjects`'s cascading class→subject picker (powers the Assessments page) also filters out retired classes in both its unrestricted and scoped branches, so they're never offered as a creation target in the first place. Deliberately did not gate `delete_subject_registration` — removing a registration is cleanup and should stay possible even after the class it references is retired, same reasoning as leaving read-only endpoints (`get_class`, `list_class_subjects`, ...) untouched. Frontend: `TargetClassPicker.svelte` (promotion), `StudentForm.svelte`'s class dropdown, and `ResponsibilitiesTab.svelte`'s class-teacher-assignment dropdown all now filter to `is_active` classes so a retired class is never offered as a pickable target to begin with, not just rejected after the fact; the class detail page's Subjects/Students tabs (`SubjectsTab.svelte`/`StudentsTab.svelte`) hide their Add/Assign controls behind a "Class inactive" notice when viewing a retired class directly, since that's the exact surface the bug report described. 8 new backend tests across `test_academic.py` (assign_subjects/class-teacher/subject-teacher), `test_student_enrollment.py` (register_subjects + create_class_assignment), `test_assessments.py`, `test_attendance.py`, and `test_promotion_validation.py`; full targeted batch (6 files) green with zero failures. `svelte-check` 0 errors, 351 warnings (unchanged from 12ay).

- 12ba: Branded subdomain login + eliminating the "school code" pain point, prompted directly by the user manually testing an invite/login flow and hitting a login page that demanded a "school code" a teacher has no natural way to know. Investigation found the fix was smaller than it looked, and led to a bigger product decision, worked through with the user via conversation before implementing (not a formal AskUserQuestion — the user pushed back on the tool-driven flow and asked for plain-English discussion instead, which shaped the rest of the session): every school should feel like the app was built just for them — type `<subdomain>.ttek-sms.com`, land on a fully branded login page, never see another school's anything or a shared "code" field. Root findings: `User.email`/`User.phone` are DB-level globally unique across the whole platform (`models/auth.py`), so `services/auth_lookup.py::find_user_by_identifier` already resolved EMAIL/PHONE logins with **no** `school_code` at all when omitted — the code box being shown unconditionally on plain-domain login (`LoginForm.svelte`) was a pure frontend artifact, not a backend requirement. `ADMISSION_ID` (student) logins are the one case that genuinely still needs a school identifier, since `admission_id` is only unique per-school. Also found the branded-subdomain experience was already ~95% built (dynamic page title/logo/motto/brand-color on login, from a prior phase) — the one gap was the browser-tab favicon, hardcoded static in `app.html`. **Decided scope** (a phased plan, written to `/home/latif/.claude/plans/linked-swimming-cook.md` and approved): Phase A (this session) — make subdomains automatic and kill the code box for staff; Phase B — production wildcard TLS via Cloudflare-proxied DNS (ops step, not app code, not executed here); Phase C — full custom-domain (BYO, `portal.school.com`) self-serve verification/provisioning, explicitly deferred until a specific school asks, since it needs domain-ownership verification and TLS provisioning (Cloudflare for SaaS "Custom Hostnames" is the standard tool) that don't exist yet — `School.custom_domain` + `GET /schools/by-domain` remain resolution-only, unverified, safe today only because school creation is superadmin-only. Implemented: (1) `services/school.py::create_school` now auto-generates a subdomain slug from the school name when left blank (`_generate_unique_subdomain` — slugify, dedupe with `-2`/`-3`..., avoid the same reserved-word set `frontend/src/lib/stores/subdomain.ts` already excludes) — every school gets a branded URL with zero admin action required, not just when someone remembers to set one. (2) New public `GET /schools/search?q=` ("Find my school," min 3 chars, capped to 10 results, matches partial name or `school_code`) for the ADMISSION_ID path when a student isn't already on their school's own subdomain — added to `core/limiter.py::_TIGHT_LIMITS` (10/min) since unlike the existing exact-subdomain lookup, a name-search endpoint really can enumerate the school directory if left unguarded. (3) `LoginForm.svelte`/`login/+page.svelte`: the school-identifier UI is now driven by live-detected login type (`detectLoginType()` on the typed identifier) instead of `!isSubdomain` — staff typing an email/phone never see any school box at all; typing something that looks like an admission number reveals a typeahead "Find my school" picker (calls the new search endpoint, debounced) instead of a bare text code field, closing over the selected result's subdomain to fetch full rich branding via the existing `getSchoolBranding()` call. (4) `applyBranding()` (`lib/api/schools.ts`, the single shared branding-application function already called from login/portal/forgot-password/Setup-preview) now also swaps `app.html`'s `#app-favicon` link to the school's logo, falling back to the default when none — every caller gets this for free. (5) Backend CORS (`main.py`) gained `allow_origin_regex=r"^http://[a-z0-9-]+\.localhost(:\d+)?$"` alongside the existing static `allow_origins` list — makes `http://<subdomain>.localhost:5173` work end-to-end locally with zero other setup, since every modern browser already resolves `*.localhost` to loopback natively (RFC 6761) and the existing dev-mode detection in `subdomain.ts`/`hooks.server.ts` already special-cases it; a real client on the public internet can never present a `.localhost` Origin, so this is safe to leave on unconditionally rather than gating it to non-production. (6) Surfaced each school's own subdomain URL where staff first encounter the system: the staff invite-result modal (`admin/staff/[id]/+page.svelte`) now shows "bookmark `https://<subdomain>.ttek-sms.com` — their school's own sign-in page, no code to remember" beside the invite link. (7) Found and fixed a real gap while doing this: `admin/setup/ProfileTab.svelte` (Setup > Profile) had **no subdomain field at all** despite the backend (`SchoolUpdate` schema, `update_school()`) fully supporting editing it — the only existing references were `schoolStore.set({subdomain: ...})` local-cache-sync lines, not an actual form control. Added a proper "Sign-in page" card (`https://` + editable slug + `.{platform domain}` suffix) wired through `updateMySchool()`; added `subdomain` to the frontend `SchoolUpdatePayload` type, which was missing it entirely. 12 new backend tests (`test_schools.py` — auto-slug blank/collision/explicit/reserved-word, search min-length/partial-name/school_code-match/excludes-inactive, and an explicit EMAIL-login-with-no-school_code regression test now that this is load-bearing UX, not just incidental backend behavior); full `test_schools.py`/`test_auth.py`/`test_staff.py` green. `svelte-check` 0 errors, 351 warnings (unchanged). Not yet done, deliberately out of scope for this pass: actually pointing production DNS/Cloudflare at the platform (Phase B, an ops action outside what's executable from a coding session), and Phase C's custom-domain verification pipeline.

- 12bb: Student password-reset dead end, prompted directly by the user's one-line follow-up to 12ba ("we can make it easy with students too"). Investigated the actual student portal onboarding/login flow rather than guessing what "easy" meant, and found something worse than friction: a silently broken flow, same shape as 12ba's school-code bug. Root cause: `services/auth_reset.py::_resolve_phone()` only checked `StaffMember.phone`/`User.phone` — neither exists for a student (`ADMISSION_ID`) account, since `Student` has no phone field of its own at all. So `forgot_password()` hit its `if not phone: return None` branch and silently skipped sending anything, but `POST /auth/forgot-password` always returns 204 regardless (by design, to prevent account enumeration) — so the frontend advanced to the OTP-entry screen every time regardless, leaving a student waiting forever for a code that was never sent, no error, no explanation. Confirmed the *initial* credential handoff (`services/student_portal.py::grant_portal_access` — admission number as both username and password, guardian SMS'd "Login with admission number as username and password") and `PortalAccessCard.svelte`'s on-screen summary were already correct and needed no changes. Fixed `_resolve_phone()` with a guardian-phone fallback for `user.student_id` accounts, reusing the exact `StudentGuardian`/`Guardian.is_primary` query `_notify_guardian()` already uses — the guardian relays the code, same real-world pattern as initial access. Also contextualized the reset SMS with the student's name (`_reset_sms_text()`, new) since a guardian might hold their own separate guardian-portal account too and shouldn't have to guess which login a code is for. Second bug found in the same investigation: `forgot-password/+page.svelte` hard-blocked the *entire* reset flow behind a resolved `schoolCode` with zero fallback — "School not detected. Please open this page from your school's URL." — never got the "Find my school" treatment the login page got in 12ba, and over-blocked EMAIL/PHONE too even though `school_code` was never actually required for those (same lesson as 12ba, just not yet propagated to this page). Fixed by mirroring `login/+page.svelte`'s already-correct shape: reactive login-type detection from the typed identifier, no `schoolCode` requirement for EMAIL/PHONE, the search picker only surfaces for the ADMISSION_ID case. Extracted the "Find my school" UI out of `LoginForm.svelte` into a new self-contained `frontend/src/lib/components/SchoolFinder.svelte` (owns its own query/debounce/results state, exposes just `onSelect(school | null)` — `null` signals "previous pick invalidated, user is typing again") so both pages share one implementation instead of a second copy silently drifting from the first, matching this codebase's own repeated convention. Deliberately not done: adding a `Student.phone` field (bigger schema change, not needed — the guardian-relay pattern already matches how this works in practice for this age group); touching the already-correct initial-grant flow; normalizing admission-number typo tolerance (flagged during investigation as a minor, speculative concern with no concrete evidence it's actually biting anyone — not worth loosening an exact-match lookup without a clearer signal). 4 new backend tests (`test_auth_reset.py` — student-with-guardian-phone now gets a real OTP via `dev_otp`, student-with-no-guardian still safely no-ops, SMS text includes the student's name, staff-phone resolution unaffected); targeted regression (`test_auth.py`/`test_auth_reset.py`/`test_students.py`/`test_staff.py`/`test_schools.py`) showed the one pre-existing [[rate_limiter_flakiness]] `429` signature, zero regressions. `svelte-check` 0 errors, 350 warnings (down 1 — incidentally fixed a missing-label a11y warning while touching the identifier input). Verified live against the real dev DB (not just the isolated test DB): created a throwaway student + guardian, called `grant_portal_access`/`_resolve_phone`/`_reset_sms_text` directly, confirmed the guardian's real phone resolved and the SMS text correctly named the student, cleaned up all test data afterward.

- 12bc: Reversed course on 12ba/12bb's "Find my school" search feature — a real product-direction correction, not a bug fix, prompted directly by the user rejecting the whole approach once they saw where it was headed. Their model: Tagnatek onboards every school individually; each school is reached *only* through its own specific URL (a subdomain assigned at onboarding, or a custom domain once the school provides one) — never a generic "log in and we'll find your school" entry point. A searchable school directory (`GET /schools/search` + the picker UI, both built in 12ba/12bb) lets anyone see which schools use the platform, directly undermining "this app was built specifically for us" and creating a real information-disclosure risk the user flagged unprompted. PWA installability plus each school linking to its own portal from its own website means nobody needs to *find* a URL after the first time, so nothing of value was actually lost by removing it. First draft of the fix (also rejected) would have restricted the existing "no school_code → unscoped lookup" branch in `find_user_by_identifier` to `is_superadmin` accounts — closing the hole, but still one shared function *inferring* which case it's in from whether a field happens to be present. The user's further correction: don't infer — platform-admin login and school login should be two genuinely separate mechanisms that never share a lookup path or a decision point. Implemented: (1) **`school_code` is now a required field** (schema-level, no default) on `LoginRequest`/`ForgotPasswordRequest`/`VerifyOtpRequest` — a request without one is a 422 from FastAPI's own validation, not a runtime branch; `find_user_by_identifier` (`services/auth_lookup.py`) simplified accordingly — resolve the school once, then scope every login_type's lookup to it uniformly (the previous EMAIL/PHONE-optional-vs-ADMISSION_ID-required split is gone, replaced by one uniform shape). (2) **New, fully separate `POST /auth/superadmin-login`** (`SuperadminLoginRequest{identifier,password,remember_me}`, no `school_code` field at all) — looks up `User.email + is_superadmin=True` directly, never calls `find_user_by_identifier`. Extracted the shared password-verify/session/token tail of the old `login()` into `services/auth.py::_complete_login()`, reused by both `login()` and the new `superadmin_login()`, so the two lookup paths stay genuinely independent while not duplicating the token-issuance logic. Also added an explicit `User.is_superadmin.is_(False)` filter to `find_user_by_identifier`'s EMAIL/PHONE queries — caught by a test failure during verification: the *test* superadmin fixture (`conftest.py::school_admin`) is deliberately school-attached for convenience elsewhere, unlike the real production superadmin (`scripts/create_superadmin.py`, `school_id=None`), so without the explicit filter a superadmin-with-a-school-id could still slip through the regular endpoint — the exclusion needed to be a real invariant, not an accident of one row's data shape. (3) **Removed the school-search feature outright**, not hidden: `GET /schools/search` (router/service/schema), its rate-limiter entry, `SchoolFinder.svelte`, and `searchSchools()`/`SchoolSearchResult` from the frontend API client all deleted. (4) `login/+page.svelte` and `forgot-password/+page.svelte`: when no school is resolved from the actual URL (bare domain — checked from `subdomain`/`customDomain`, deliberately *never* from a cached `localStorage` session, since identity must be tied to the URL, not to browser storage, or a remembered school would let a stale form render on the wrong domain), neither page renders a functional form at all — just "This sign-in page is specific to your school. Please use your school's own link." `frontend/src/routes/+page.svelte` (platform-admin) switched to the new `superadminLogin()` client function. (5) **Test suite ripple** (the bulk of this session's effort, flagged to the user in advance as a real cost before starting): `school_code` being schema-required broke every test helper that logged in a fresh non-superadmin staff member without one — roughly 20 files' `_login_as_*`/`_other_school_auth`-style helpers needed `"school_code": school.school_code` added (mechanical, one line each); a handful of helpers that create a *second-school superadmin* for cross-tenant isolation tests (`test_academic.py`, `test_documents.py`, `test_attendance.py`, `test_permissions.py`) were switched to the new `/auth/superadmin-login` endpoint instead, since that's what they actually are. `conftest.py`'s central `auth` fixture (used by nearly every test in the suite) switched to `/auth/superadmin-login`. `test_auth.py` rewritten with `school`-scoped fixtures and new tests for the two-endpoint split (superadmin login succeeds/regular-user-rejected, regular login succeeds/superadmin-rejected/missing-school_code-422/bogus-school_code-404/wrong-school-401). `test_schools.py`'s search-endpoint tests deleted; its no-school-code-succeeds test flipped to a requires-school-code-422 test. First full-suite run caught two genuinely missed call sites (`test_permissions.py`, single-quoted `'/auth/login'` — my first grep sweep only matched double-quoted strings) — fixed once found; every subsequent full-suite run showed only the pre-existing [[rate_limiter_flakiness]] `429` signature (confirmed three separate runs, always the exact same class of failure — high-login-count test files like `test_assessment_scope.py` at 16 logins/file exceeding the 10/min budget when run standalone-fast — never a logic failure; individually-run samples from every affected file passed cleanly). `svelte-check` 0 errors, 349 warnings. Live-verified against the running dev stack: `/auth/login` 422s with no `school_code`, 404s with a bogus one; `/auth/superadmin-login` 401s cleanly on bad creds; `/schools/search` no longer serves search results (the URL now just falls through to the unrelated `{school_id}` catch-all route, correctly superadmin-gated — confirms no directory data is reachable, not a routing bug).

- 12bd: Superadmin school-onboarding screen, asked for directly ("what is next?" → I flagged that `superadmin/+page.svelte` was a literal "coming soon" placeholder despite three sessions (12ba–12bc) building the entire "every school gets its own URL, onboarded individually by Tagnatek" model with no actual UI to do that onboarding — the user agreed to build it next). Pure frontend build — the backend (`POST /schools`, `GET /schools`, `GET /schools/regions`, `GET /schools/districts`) already existed and needed zero changes, including the 12ba auto-subdomain-generation behavior. Scope deliberately limited to list + create (discussed and agreed upfront) — editing/deactivating a school or a custom-domain management flow explicitly deferred, since `GET/PATCH /schools/{id}` already exist on the backend whenever that's wanted next. Added `listSchools()`/`createSchool()`/`listRegions()`/`listDistricts()` to `lib/api/schools.ts` (none of this — including the regions/districts endpoints — had any frontend consumer at all before now); new `superadmin/SchoolForm.svelte` (region→district cascading dropdown, first real use of that reference data in the frontend) mirroring `admin/staff/StaffForm.svelte`'s established drawer conventions (unsaved-changes guard, `ConfirmModal` on discard); rewrote `superadmin/+page.svelte` from the placeholder into a real schools table (name/code/type/sign-in-link/status) with an "Onboard school" button. On successful creation, the new school's sign-in link is shown immediately with a copy button, mirroring the staff-invite-result pattern from 12ba (same `PUBLIC_PLATFORM_DOMAIN` env convention). `svelte-check` 0 errors, 349 warnings (unchanged). Verified live against the real dev stack: since the real superadmin's password isn't known to me, verified by minting a JWT directly for the real `admin@tagnatek.com` row the same way the login endpoint would after a successful password check (`core.auth.create_access_token`, no password bypass of any check that actually gates real users) — confirmed `GET /schools/regions`/`GET /schools` return real data, `POST /schools` with a blank subdomain correctly auto-generated one (`verify-onboarding-school`) end-to-end through the exact payload shape the new form sends; cleaned up the throwaway school afterward, confirmed gone by re-querying.

- 12be: Follow-up to 12bd — edit/deactivate a school, plus superadmin password change, both explicitly deferred in 12bd and asked for directly this session. Found a real gap while building "Deactivate": `services/auth_lookup.py::resolve_school_id()` never filtered `School.is_active` at all — deactivating a school (already possible via raw `PATCH /schools/{id}`, just with no UI) only hid it from `list_schools`/branding lookups, it did **not** actually block sign-in — a user who already had the subdomain/URL cached could still log in against a "deactivated" school. Fixed by adding `School.is_active.is_(True)` to the one query `resolve_school_id` runs; since every login/forgot-password/verify-otp call funnels through `find_user_by_identifier` → `resolve_school_id`, this one-line fix makes deactivation a real, immediate access block for every account at that school — confirmed there were no other callers of `resolve_school_id` to worry about. New regression test (`test_auth.py::test_login_rejected_for_deactivated_school`). Extended `superadmin/SchoolForm.svelte` to a dual create/edit component (`school?: SchoolRead | null` prop — present means edit) rather than a second copy: prefills from the target school, disables `school_code`/`school_type` (immutable post-creation per the backend's own `SchoolUpdate` schema), and edit-mode-only exposes `custom_domain` (self-declared, unverified — matches the existing subdomain-editing precedent, no verification/TLS automation built, that's still the explicitly-deferred bigger Phase C) and an `is_active` toggle with an inline amber warning when unchecking it, rather than a separate confirm-modal — judged sufficient since "Save changes" is itself already a deliberate, reviewable step. Also had to widen the frontend `SchoolRead` type (`lib/api/schools.ts`) with `region_id`/`district_id`, which the backend schema always had but the frontend type was missing entirely (never needed until an edit form had to prefill a region→district cascade from existing data). `superadmin/+page.svelte` gained an Edit button per row and a "Change password" button. For password change: found `POST /auth/change-password` (`services/auth.py::change_password`) was already fully generic — keyed on `user_id`, no staff/school assumptions — and the frontend already had a complete, working implementation at `(app)/change-password/+page.svelte`, just trapped inside the `(app)` layout a superadmin (no `school_id`, no sidebar/branding context) shouldn't route through. Extracted the card itself into shared `lib/components/ChangePasswordForm.svelte` (`showForgotLink`/`onSuccess` props — superadmin's modal omits the forgot-password link, since that flow requires a `school_code` a superadmin doesn't have) rather than writing a second copy; the `(app)` page now just wraps it, byte-identical behavior. Superadmin's version renders in a small centered modal matching `ConfirmModal.svelte`'s dialog skeleton. `svelte-check` 0 errors throughout (349 warnings, unchanged baseline). Verified live against the real dev stack using the same minted-JWT approach as 12bd (never touched the real superadmin's actual password — didn't want to change live credentials just to prove the wiring, and the underlying `change_password`/`changePassword()` behavior was already covered by existing passing tests untouched by this refactor): created a throwaway school, edited its address and flipped `is_active` off via the real `PATCH` endpoint, confirmed its branding endpoint 404'd and a real login attempt against it 404'd (not 401) instead of succeeding, cleaned up the throwaway school afterward. Targeted backend suite green (`test_auth.py`/`test_auth_reset.py`/`test_schools.py`/`test_students.py`/`test_staff.py`/`test_portal.py`/`test_guardian_portal.py`) — one pre-existing [[rate_limiter_flakiness]] `429`, zero regressions. Still deliberately out of scope: full custom-domain verification/TLS provisioning (Phase C from 12ba, unchanged).

- 12bf: Platform-admin login dead end, reported directly by the user immediately after 12be ("I can't sign in to the platform site again, it keeps redirecting to /login with [the school-specific message]"). Root cause was in `lib/api/client.ts`'s shared axios interceptor, not anything backend — a pre-existing latent bug that 12bc's "no functional form off a school's own URL" change turned from harmless into a hard dead end. The response interceptor treated **every** 401 as "the access token probably expired," including a plain wrong-password response from `/auth/login`/`/auth/superadmin-login` themselves — it would try to silently refresh (which always fails during an actual login attempt, since there's no session yet to refresh), then `clearAuth()` and hard-redirect to `/login`. Before 12bc this was merely annoying (a failed login silently bounced you to a page that still happened to work); after 12bc, `/login` shows no form at all off a school's subdomain, so a superadmin's wrong-password attempt on `/` — or even a stale leftover token from earlier testing sitting in localStorage — became a full dead end with the real "Invalid credentials" error never reaching the form. Fixed by excluding the public, inherently-unauthenticated auth endpoints (login, superadmin-login, refresh, forgot-password, verify-otp, reset-password, accept-invite, invite-info) from both interceptors entirely (`isPublicAuthPath()`) — a 401 from any of these now always propagates straight to the caller's own error handling, exactly as it always should have. Separately hardened the *real* session-expiry path too (found while fixing the above, same file): `loginPathFor()` now sends a dying superadmin session back to `/` instead of `/login`, since `/login` was never the right recovery page for an account with no school at all — persisted via a `localStorage['is_superadmin']` flag set in `stores/auth.ts::setAuth()` (not just kept on the in-memory `user` object, which isn't populated until `/auth/me` resolves in `+layout.svelte`'s `onMount` — too late if a token-refresh failure fires first on a fresh page load). Backend was never involved — confirmed `test_auth.py` green unchanged, this was purely a frontend interceptor bug. `svelte-check` 0 errors. Told the user their browser's existing localStorage may still hold a stale token from earlier testing predating this fix and to hard-refresh/clear site data once to fully recover, since the fix itself can't retroactively repair state already sitting in their browser.

- 12bg: Second, deeper cause of the same platform-admin lockout — 12bf's interceptor fix wasn't the whole story, the user correctly pushed back ("why will a platform admin have access to a school that doesn't exist") after still being stuck. Real culprit: `hooks.server.ts`'s `handle()` had a server-side forced redirect — `if ((subdomain || customDomain) && event.url.pathname === '/') throw redirect(302, '/login')` — intended to stop the generic platform-admin form from appearing on a real school's own branded URL. The bug: `parseHost()` is a pure syntactic guess from the `Host` header alone, with **no check against the database at all** — any hostname that isn't the exact literal `localhost`/`127.0.0.1`/the configured platform domain falls through to "everything else is a potential custom domain" and gets treated as a real school unconditionally. So reaching the dev server through anything other than that exact string (a WSL-forwarded address, a LAN IP, any local alias) silently redirected `/` straight to `/login` for a school that was never real — and since 12bc made `/login` refuse to render anything for a nonexistent school, this made the platform-admin page completely unreachable, with no path back, regardless of the 12bf interceptor fix (the redirect fires server-side, before the page or any client JS ever runs). Fixed by removing the forced redirect entirely — `event.locals.subdomain`/`customDomain` are still computed and set for whatever else reads them, only the `/`-specific redirect is gone. Traded a theoretical minor cosmetic case (someone manually typing `/` on a real school's own URL and seeing the unbranded admin form instead of that school's branding — not a real user flow, nobody links to `/` from within a school's own pages) for actually keeping platform-admin access reachable regardless of which hostname/IP happens to reach the server. `svelte-check` 0 errors. Verified live: `curl` with `Host: 172.20.10.5:5173` (simulating exactly the kind of non-canonical address that used to trigger this) against `/` now returns a clean 200 with no `Location` header, where it previously 302'd to a `/login` dead end.

- 12bh: Third bug in the same area, reported directly ("when you signin and get to /superadmin it works fine but removing the 'superadmin' [i.e. navigating to /] sends me /dashboard with a school I never created"). Unrelated to 12bf/12bg — a pre-existing bug, not introduced this session, just never previously exercised. `routes/+page.svelte`'s (root, platform-admin login) "already signed in, skip the form" `onMount` check — `if (get(isAuthenticated)) goto('/dashboard')` — never checked `is_superadmin` at all, unconditionally sending *any* authenticated session to `/dashboard`, inconsistent with `handleLogin()`'s own success branch two lines below it (`goto(user.is_superadmin ? '/superadmin' : '/dashboard')`) and with `login/+page.svelte`'s equivalent already-signed-in check (which correctly calls `getMe()` and branches). A superadmin has no `school_id` at all, so `/dashboard` rendered whatever school context happened to be available/cached rather than erroring — reading as "a school I never created" rather than a clear failure. Fixed `/`'s onMount to match the established `getMe()`-then-branch pattern already used at `login/+page.svelte`. Also closed the same gap from the other direction as defense-in-depth: `(app)/+layout.svelte` already had a guard bouncing a portal user (student/guardian login) *out* of the staff app shell to `/portal` if they landed there directly (e.g. a stale bookmark) — added the mirror-image guard for `is_superadmin` → `/superadmin`, so navigating to `/dashboard` (or any `(app)` route) directly, not just via `/`'s own redirect, is covered too. `svelte-check` 0 errors, 349 warnings (unchanged). Three related-but-distinct login/routing bugs surfaced back-to-back this session (12bf interceptor, 12bg hooks.server.ts redirect, 12bh this one) — none introduced by the 12ba–12be work itself, all pre-existing gaps that the "every login must resolve to a real, specific place" tightening in 12bc made newly visible/blocking instead of silently working around.

- 12bi: Fixed a visible flash of `/dashboard` before correctly landing on `/superadmin` on page reload, reported right after 12bh shipped ("It works but when you takeway the superadmin and reload the page it goes to my school dashbaord and redirect back"). First pass (incomplete): `/+page.svelte`'s own `onMount` (added in 12bh) made an independent `getMe()` call to decide where to send an already-authenticated visitor, racing the root `+layout.svelte`'s own separate `getMe()` call which populates the canonical `currentUser` store — replaced with a `$effect` reacting to that same canonical store (one call, one source of truth) and dropped `handleLogin()`'s now-redundant explicit post-login `goto()`. User reported the flash persisted after this — correctly, since the real culprit was a file not yet touched: `frontend/src/routes/+page.ts` had its own `load()` function, which SvelteKit runs *before* the page component (and any of its effects) ever mounts — it redirected to `/dashboard` on bare access-token presence in localStorage alone, no `is_superadmin` check at all, and a superadmin's own login also stores an access token. This fired unconditionally ahead of, and independent from, the component-level fix, sending straight to `/dashboard` before `(app)/+layout.svelte`'s superadmin guard caught it a moment later and bounced back — the actual flash. Fixed by deleting the `load()` redirect entirely, leaving `+page.ts` with just `export const ssr = false`; routing now happens in exactly one place (`+page.svelte`'s `$effect` on `currentUser`, populated once by the root layout's single `getMe()` call), with nothing upstream of it left to race or preempt it. `svelte-check` 0 new errors (349 warnings, all pre-existing). Lesson for next time this class of "redirect loop"/"wrong page after auth" bug shows up: check `+page.ts`/`+layout.ts` `load()` functions first, not just component-level `onMount`/`$effect` code — `load()` runs earlier in the lifecycle and a redirect there fully preempts anything the component would otherwise decide.

- 12bj: A school's own subdomain root showed the platform-admin login instead of that school's branded login, reported directly ("When I open a private browser and type this 'http://shs.localhost:5173/', it send me to platform"). The sign-in link every school is actually given (staff-invite modal, superadmin school list) is exactly `https://<subdomain>.<platform domain>` — the bare root — but `/` had been hardcoded to always render the platform-admin login regardless of hostname ever since 12bg removed the old forced redirect entirely (to fix a *different* bug: that redirect used to fire on *any* unrecognized hostname, including a LAN IP/WSL address falling into `parseHost()`'s `customDomain` catch-all, making platform-admin unreachable with no way back). Re-added a redirect in `hooks.server.ts`, scoped narrowly this time: only when `subdomain` is set (a real match against the explicit `.localhost`/`.{PLATFORM_DOMAIN}` suffix rule — a known, deliberate convention, not a guess) does `/` now redirect to `/login`; the risky `customDomain` catch-all branch is deliberately left alone, so the 12bg fix (platform-admin stays reachable from a non-canonical hostname) still holds. Real custom-domain schools landing on platform-admin at their own root instead of `/login` remains a known, accepted gap until Phase C's domain-verification pipeline exists (12ba) — `customDomain` has no DB check to safely redirect on yet. `svelte-check` 0 new errors. Verified live against the running dev stack: `Host: shs.localhost:5173` on `/` now 302s to `/login`; bare `localhost:5173` and a non-canonical IP host both still 200 with no redirect, confirming the 12bg fix wasn't undone.

- 12bk: Removed every hardcoded `ttek-sms.com` fallback, reported directly ("I have not bought 'ttek-sms.com' and is like you have hardcoded that into subdomain which is not good"). A full grep found two different problems: (1) a real bug — `services/email_notifications.py`/`sms_notifications.py`'s report-published guardian notification text hardcoded `.ttek-sms.com` directly into the message with no config lookup at all, unconditional, not even a fallback; in production this would SMS/email real guardians a login link pointing at a domain nobody owns. (2) a landmine, not yet firing — 5 frontend files repeated the same `env.PUBLIC_PLATFORM_DOMAIN ?? 'ttek-sms.com'` pattern, silently substituting the fake domain whenever the env var isn't set (which it wasn't, in this exact codebase's own `.env`, confirmed while fixing). Researched multi-tenant SaaS domain-config practice before implementing, since "make it dynamic" (the user's phrase) could reasonably mean an env var or a database-backed superadmin setting — concluded env var is correct and already the established pattern here (matching `APP_BASE_URL`/`FRONTEND_BASE_URL`), since the platform's own root domain is a single global value tied to real DNS/TLS provisioning regardless of where it's stored, and `hooks.server.ts` runs on every request with zero DB access today — a DB-backed value would mean a network round-trip or cache-invalidation machinery for something that in practice is set once at deploy and never changes. Per-school custom domains are the part that's genuinely per-tenant-dynamic, and that's already correctly database-backed (`School.custom_domain`) — untouched by this fix. Implemented: `core/config.py` gained `platform_domain: str = ""`; the two notification functions now build the real link only when it's set, otherwise drop that clause entirely ("Check with your school for how to view it.") rather than sending a broken URL. New shared `frontend/src/lib/platformDomain.ts` (`getPlatformDomain()`/`schoolLoginUrl()`) replaces the 5 repeated inline fallbacks — `hooks.server.ts`/`lib/stores/subdomain.ts` (server/detection-only, no shared helper needed there) just drop the fallback so an unconfigured platform domain falls through to the existing `customDomain` catch-all exactly like any other unrecognized hostname; `superadmin/+page.svelte`, `admin/staff/[id]/+page.svelte`, `admin/setup/ProfileTab.svelte` degrade gracefully in the UI (hide the link/copy button, show a "platform domain not configured yet" note) instead of ever rendering a fake URL. `.env.example` and the real dev `.env` both changed from `PLATFORM_DOMAIN=ttek-sms.com` to unset/placeholder, with a comment explaining local `*.localhost` subdomain testing is unaffected either way. Backend/frontend containers force-recreated (a plain `restart` doesn't re-read `env_file`) to actually pick up the cleared value — confirmed live via `settings.platform_domain` inside the container. Targeted backend suite (`test_sms.py`/`test_schools.py`/`test_auth.py`/`test_assessments.py`/`test_scoring_lock.py`) green — no test asserted the old hardcoded string. `svelte-check` 0 new errors. Verified live post-recreate: `shs.localhost:5173/` still 302s to `/login` (12bj unaffected — that redirect only consumes the already-resolved subdomain value, agnostic to how the platform-domain match fired), `/login` still 200 on that subdomain, no errors in either container's logs.

## Phase 0 checklist
- [x] Folder structure created
- [x] CLAUDE.md written
- [x] .gitignore
- [x] .env.example
- [x] docker-compose.yml
- [x] backend/requirements.txt
- [x] backend/Dockerfile
- [x] backend/app/main.py
- [x] backend/app/core/config.py
- [x] backend/app/core/database.py
- [x] backend/app/core/redis.py
- [x] backend/app/models/base.py
- [x] alembic.ini + alembic/env.py
- [x] frontend/package.json
- [x] frontend/Dockerfile
- [x] .github/workflows/ci.yml
- [ ] Seed data: GhanaRegion, GhanaDistrict, GhanaPublicHoliday
- [ ] Rate limiting middleware
- [ ] ARQ worker setup
- [ ] Sentry integration
- [ ] docker compose up → all services healthy
- [x] pytest passes (4 tests, all green)

## Phase 0 milestone
Docker up. DB migrating. Redis running. ARQ worker processing test job.
Rate limiting active. CI passing. Sentry live. Local storage folder structure created.

## Phase 1 checklist
- [x] All 10 model files (Groups 1–10)
- [x] core/auth.py — JWT + password hashing + token rotation
- [x] core/permissions.py — 3-layer resolution + Redis cache
- [x] schemas/school.py + schemas/auth.py
- [x] services/auth.py + services/school.py
- [x] routers/auth.py + routers/schools.py
- [x] Routers registered in main.py
- [x] alembic/env.py updated to import all models
- [x] scripts/seed_reference_data.py — regions, districts, holidays, positions
- [x] scripts/create_superadmin.py
- [x] tests/test_auth.py (runs in Docker against PostgreSQL)
- [x] docker compose up → alembic upgrade head succeeds
- [x] seed scripts run successfully
- [x] pytest passes (12 tests, all green)

## Phase 1 milestone
All models migrated. Auth endpoints live. Superadmin can log in.
School registration works. Reference data (regions, districts, holidays, positions) seeded.

## How to run
```bash
docker compose up -d --build
docker compose exec api alembic upgrade head
docker compose exec api python scripts/seed_reference_data.py
docker compose exec api python scripts/create_superadmin.py
docker compose exec api pytest -v
```

### Testing a school's branded subdomain locally
Every modern browser resolves `*.localhost` to loopback natively (RFC 6761) — no `/etc/hosts` edit, no real DNS, no wildcard TLS needed. Visit `http://<subdomain>.localhost:5173` (e.g. `http://achimota-school.localhost:5173`) and the login page renders that school's logo/name/motto/brand-color/favicon exactly like it would on `<subdomain>.ttek-sms.com` in production — `frontend/src/lib/stores/subdomain.ts`/`hooks.server.ts` already detect `*.localhost` the same way, and the backend's CORS config (`main.py`) explicitly allows it via `allow_origin_regex`. Find (or set) a school's subdomain via Setup > Profile, or check `School.subdomain` directly — every school gets one auto-assigned on creation now (see 12ba).

## Completed phases
- Phase 1 — Core Models + Auth + School Setup (2026-06-11)
- Phase 2 — Academic Structure (2026-06-12)
- Phase 3 — Staff Profiles + Bulk Import (2026-06-12)
- Phase 4 — Students & Enrollment + Bulk Import (2026-06-12)
- Phase 5 — Housing (2026-06-13)
- Phase 6 — Fees (2026-06-13)
- Phase 7 — Report Cards & Documents (2026-06-13)
- Phase 8 — Attendance (2026-06-13)
- Phase 9 — Assessments & Scoring (2026-06-13)
- Phase 10 — Offline Sync (2026-06-13)
- Phase 11 — SMS Notifications (2026-06-13)

## Phase 6 checklist
- [x] schemas/fees.py — FeeType, FeeStructure, BulkAssignResult, FeeRecordRead, FeeSummaryRead, FeePayment, FeeDiscount (XOR validator), InstalmentPlan
- [x] services/fees.py — fee types, structures, bulk assign, records, summary
- [x] services/fees_payment.py — payments, discounts, instalment plans
- [x] routers/fees.py — 16 endpoints registered
- [x] main.py — fees router included
- [x] alembic migration e5f0a2d91c74 — PostgreSQL trigger for StudentFeeSummary (fires on student_fee_record, fee_payment, fee_discount DML)
- [x] tests/test_fees.py (fee types, structures, bulk assign, records, summary 404)
- [x] tests/test_fees_payment.py (payments, discounts XOR, instalments, summary trigger)

## Phase 6 milestone
Fee types, structures, bulk assignment, payments, discounts, and instalment plans live.
StudentFeeSummary kept in sync by PostgreSQL trigger. Balance computed at read time, never stored.

## Models migration status
All migrations applied through e5f0a2d91c74 (Phase 7 — no new migration needed).

## Phase 7 — Report Cards & Documents (COMPLETE)
Dependency: Phase 6 ✓

Milestone: PDF report cards generated on demand, QR-verified against live data. Documents uploaded and tracked.

Key constraint: Report cards are NEVER stored — generated fresh from Score + Behaviour + Attendance + GradingScale on every request. QR token verifies against live DB, not a cached PDF.

### Checklist
- [x] schemas/assessments.py — BehaviourRecord, ScoreLineRead, ReportCardData, BulkReportRequest, BulkReportJobRead
- [x] schemas/documents.py additions — DocumentRecordRead, ImportBatchResult
- [x] services/behaviour.py — StudentBehaviourRecord CRUD
- [x] services/report_card.py — assemble data (scores + grades + attendance + behaviour + rank)
- [x] templates/report_card.html — single unified template (Phase 12 review discarded the earlier Basic/SHS/ECM 3-format split — is_early_years auto-detected from class level switches the milestone-vs-numeric section, no manual format picker)
- [x] services/pdf.py — WeasyPrint render; returns bytes, never writes to disk
- [x] services/qr.py — HMAC-SHA256 token generation + verification
- [x] routers/report_cards.py — GET /report-cards/{enrollment_id}, POST /report-cards/bulk, GET /verify/{token}, GET /report-cards/bulk/{job_id}/download
- [x] services/documents.py — DocumentRecord upload (certificates, letters, photos); local /uploads now → R2 later
- [x] routers/documents.py — upload, list, download, delete
- [x] services/bulk_report_job.py — ARQ background job; generates whole-class PDFs, zips, stores ZIP path
- [x] routers/portal.py — GET /portal/report-cards/{enrollment_id} (ADMISSION_ID login only, gated by Assessment.is_published)
- [x] main.py — report_cards, documents, portal routers registered
- [x] alembic migration — no schema changes needed (StudentBehaviourRecord + DocumentRecord already in models)
- [x] tests/test_report_cards.py — behaviour CRUD, single PDF, invalid format, QR verify/tamper
- [x] tests/test_documents.py — upload, disallowed mime, list, download, delete, wrong-school empty list
- [x] tests/test_portal.py — staff rejected, unpublished 403, published PDF, cross-student 404

### Phase 7 milestone
Report cards rendered on demand by WeasyPrint. QR code on each card verifies live. Bulk class job via ARQ. Documents uploaded and tracked. Parent portal gated by Assessment.is_published.

## Phase 8 — Attendance (COMPLETE)
Dependency: Phase 7 ✓

Milestone: School schedule, calendar generation, daily attendance marking, and per-student summary live.

Key constraint: AttendanceRecord always uses school_calendar_id FK — never a raw date field.
Calendar generation is idempotent; PUBLIC_HOLIDAY beats schedule; default is Mon–Fri if no SchoolSchedule rows.

### Checklist
- [x] schemas/attendance.py — ScheduleUpsert, ScheduleRead, CalendarGenerateRequest, CalendarDayRead, CalendarDayOverride, AttendanceMark, AttendanceMarkRequest, AttendanceRecordRead, AttendanceSummaryRead
- [x] services/attendance_calendar.py — upsert_schedule, list_schedule, generate_calendar (idempotent), list_calendar, override_calendar_day
- [x] services/attendance.py — mark_attendance (SCHOOL_DAY/EXAM_DAY/HALF_DAY gate, upsert), list_attendance, get_summary
- [x] routers/attendance.py — 8 endpoints: schedule CRUD, calendar generate/list/override, mark, records, summary
- [x] main.py — attendance router registered
- [x] tests/test_attendance.py — schedule upsert + idempotent, calendar generate (weekend check) + idempotent, list, override, mark, holiday rejected, re-mark updates, list records, summary

### Business rules enforced
- Cannot mark attendance on PUBLIC_HOLIDAY, WEEKEND, or SCHOOL_HOLIDAY days (422)
- Re-submitting attendance for same student+calendar+period updates the record
- period_id IS NULL for daily (whole-day) attendance

### Phase 8 milestone
School schedule configurable per weekday. Calendar generated idempotently per term with PUBLIC_HOLIDAY beating schedule. Daily attendance marked and re-markable. Per-student term summary with attendance rate.

## Phase 9 — Assessments & Scoring (COMPLETE)
Dependency: Phase 8 ✓

Milestone: Grading scales, assessment types, assessments, and the full score entry → approval flow live.

Key constraints:
- Grade is NEVER stored on Score — resolved at approval time from the school's default GradingScale
- cached_grade_label is set on approval; cleared when GradingScale bands change
- ScoreAuditLog written on every score create/update
- Re-submitting a score resets is_approved=False and clears cached_grade_label

### Checklist
- [x] schemas/assessments.py additions — GradeCreate/Read, GradingScaleCreate/Read, AssessmentTypeCreate/Read, AssessmentCreate/Read, ScoreSubmit, BulkScoreSubmit, ScoreRead, ScoreApproveRequest
- [x] services/grading.py — GradingScale CRUD, Grade band add/delete, resolve_grade(), clear_cached_grades()
- [x] services/assessment.py — AssessmentType CRUD, Assessment CRUD, publish_assessment()
- [x] services/scoring.py — submit_scores (bulk upsert + audit log), approve_scores (resolve grade → cached_grade_label), list_scores
- [x] routers/assessments.py — 13 endpoints; permissions: approve_scores / enter_scores / view
- [x] main.py — assessments router registered
- [x] tests/test_assessments.py — grading scale CRUD, grade band validation, assessment type dedup, assessment create/publish, score submit/range check/resubmit, approve → grade label, list scores

### Permission map
- assessments.approve_scores → grading scale management, assessment type/assessment CRUD, score approval, publish
- assessments.enter_scores → submit scores
- assessments.view → read-only all

### Phase 9 milestone
Teachers enter scores; HODs approve; cached_grade_label set from GradingScale on approval. Report card service already reads cached_grade_label — no changes to Phase 7 code needed.

## Phase 10 — Offline Sync (COMPLETE)
Dependency: Phase 9 ✓

Milestone: Dexie WriteOutbox items processed server-side; concurrent edit conflicts detected, logged, and resolvable.

Key constraint: offline_session_started_at is sent with every outbox item. If Score.submitted_at > offline_session_started_at, the server received a newer write after the session started → OfflineSyncConflict written.

### Checklist
- [x] schemas/sync.py — OutboxScoreData, OutboxItem, OutboxSyncRequest, OutboxItemResult, ConflictRead, ConflictResolveRequest
- [x] services/sync.py — process_outbox (per-item score sync with conflict check), list_conflicts (user-scoped, unresolved only), resolve_conflict (CLIENT_WINS re-applies; SERVER_WINS/DISCARDED mark resolved; MERGED applies merged_data)
- [x] routers/sync.py — POST /sync/outbox, GET /sync/conflicts, POST /sync/conflicts/{id}/resolve
- [x] main.py — sync router registered
- [x] tests/test_sync.py — new score applied, server-older applied, conflict detected, list empty + shows unresolved
- [x] tests/test_sync_resolve.py — SERVER_WINS, CLIENT_WINS (verifies score updated), DISCARDED, double-resolve 409

### Conflict resolution actions
- CLIENT_WINS → re-applies client score via _apply_score; writes ScoreAuditLog
- SERVER_WINS / DISCARDED → marks resolved; server data unchanged
- MERGED → applies caller-supplied merged_data score

### Phase 10 milestone
Offline score submissions safely merged. Conflicts surfaced to the teacher for manual resolution. Double-resolve rejected with 409.

## Phase 11 — SMS Notifications (COMPLETE)
Dependency: Phase 10 ✓

Milestone: Per-school SMS provider selection, credential management, automatic parent notifications, and manual send live.

Key design: Each school independently chooses and configures its SMS provider. Only one provider is active at a time. Activating a new provider deactivates all others automatically.

### Architecture
- SmsDriver ABC + 5 concrete drivers (AfricasTalking, Hubtel, Arkesel, WiGal, Twilio) in services/sms_driver.py
- Credentials: api_key (always required) + api_secret (where needed per provider) — stored in sms_config table, never returned by any GET endpoint
- Phone normalization: Ghana 0XXXXXXXXX → +233XXXXXXXXX (E.164 format)
- SmsLog table: every send attempt logged with status, error, entity_type/entity_id for traceability
- Fire-and-forget pattern: notification functions catch all exceptions — SMS failure never rolls back a fee payment, attendance mark, or assessment publish

### Checklist
- [x] models/school.py — SmsStatus enum + SmsLog model added
- [x] alembic/versions/a3f9c1e87b20 — sms_log table migration
- [x] services/sms_driver.py — SmsDriver ABC, SmsResult, 5 drivers, _normalize_phone(), build_driver() factory
- [x] services/sms_notifications.py — get_active_driver(), _primary_guardian_phone(), _log_result(), notify_fee_receipt(), notify_attendance_absent(), notify_report_published(), send_manual()
- [x] services/school_config.py — list_sms_configs(), activate_sms_provider() (deactivates others), delete_sms_config()
- [x] schemas/sms.py — SmsActivateRequest, SmsSendRequest (160-char validator), SmsSendResult, SmsSendResponse, SmsLogRead
- [x] routers/sms.py — 6 endpoints: POST/GET configs, POST activate, DELETE config, POST send, GET logs
- [x] services/fees_payment.py — notify_fee_receipt() wired after record_payment() flush
- [x] services/attendance.py — notify_attendance_absent() wired for ABSENT marks
- [x] services/assessment.py — notify_report_published() to all enrolled students on publish
- [x] main.py — sms router registered
- [x] tests/test_sms.py — phone normalization, config CRUD, activate (only one active), delete, send-no-config 503, message >160 chars 422, send-with-mock, log endpoint

### Automatic notification triggers
- Fee receipt: fires after services/fees_payment.py::record_payment() → primary guardian
- Absence alert: fires in services/attendance.py::mark_attendance() for ABSENT status → primary guardian
- Report published: fires in services/assessment.py::publish_assessment() → all enrolled students' primary guardians

### Provider credential fields
| Provider        | api_key           | api_secret       | sender_id      |
|-----------------|-------------------|------------------|----------------|
| AFRICAS_TALKING | AT API key        | AT username      | Sender name    |
| HUBTEL          | Client ID         | Client secret    | Sender name    |
| ARKESEL         | Arkesel API key   | (not used)       | Sender name    |
| WIGAL           | WiGal API key     | (not used)       | Sender name    |
| TWILIO          | Account SID       | Auth Token       | E.164 number   |

### Phase 11 milestone
Each school picks and configures its own SMS provider. Credentials stored server-side and never exposed via API. Automatic notifications fire on fee payments, absences, and report card publishing. Full audit trail in sms_log. Manual send available to school HEAD via POST /sms/send.

## Key decisions log
See blueprint/ttek_sms_blueprint_v4.html for full decisions.
Short version:
- One school = one record (BASIC or SHS, never both)
- Three login types: EMAIL / PHONE / ADMISSION_ID
- 6 seeded StaffPosition templates, 29 permissions across 9 modules
- SchoolCalendar gates attendance — impossible to mark on holiday
- TermEnrollment created by class teacher when student physically reports
- Dexie: WriteOutbox (write queue) — a structured read-side cache was scaffolded early on but never built out and was removed in 12ak; scores are the only entity that goes offline
- Conflict detection via offline_session_started_at on every outbox sync
- YearEndProcess bulk graduation → GraduationRecord per student
- StudentFeeSummary materialized by DB trigger for dashboard performance
- Score.cached_grade_label stored on approval, invalidated on GradingScale change
